# Архитектура проекта глобальной стратегии

## Максимально оптимизированная архитектура карты, симуляции и интерфейса

Документ фиксирует архитектуру проекта для глобальной пошаговой стратегии с физической картой, клетками, провинциями, регионами, городами, морскими зонами, движением юнитов, логистикой, экономикой и запечёнными визуальными слоями карты.

Главная цель архитектуры — не просто «чтобы работало», а чтобы проект можно было расширять годами без превращения кода в хаос.

---

# 1. Главная идея архитектуры

Игра должна быть разделена на несколько независимых слоёв:

```text
Визуал карты
Данные карты
Состояние партии
Системы симуляции
AI
UI
Сохранения
```

Главный принцип:

```text
Godot показывает игру.
Логика игры живёт отдельно от сцен, кнопок, картинок и анимаций.
```

То есть Godot отвечает за:

```text
отрисовку карты
клики мышкой
окна интерфейса
анимации юнитов
подсветку клеток
визуальные эффекты
```

А игровое ядро отвечает за:

```text
клетки
провинции
города
площадь
население
экономику
движение
логистику
войну
события
AI
```

Нельзя смешивать эти вещи в одном `GameManager`.

---

# 2. Общая схема проекта

```text
Content / Balance Data
        ↓
Static World Data
        ↓
Runtime World State
        ↓
Simulation Systems
        ↓
Events / Results
        ↓
UI + Map Renderer + Animations
```

## 2.1 Content / Balance Data

Это таблицы баланса и правила:

```text
рельефы
покровы
почвы
климаты
ресурсы
освоения
инфраструктура
модификаторы движения
модификаторы еды
модификаторы населения
модификаторы торговли
модификаторы снабжения
```

Пример:

```json
{
  "hills": {
    "defense_value": 20,
    "movement_cost": 1.25,
    "pasture_potential": 20,
    "vineyard_potential": 15,
    "road_cost": 1.25
  }
}
```

Важно: модификаторы должны лежать в таблицах, а не быть зашитыми в код.

---

## 2.2 Static World Data

Это неизменяемая география мира.

Сюда входит:

```text
клетки
полигоны
площади
центры клеток
соседства
переходы между клетками
провинции
регионы
зоны
макрорегионы
суперрегионы
континенты
морские зоны
стартовые города
стартовые дороги
стартовые реки
```

Эти данные не должны меняться во время партии.

Пример:

```gdscript
CellStatic {
  id
  polygon_points
  area_km2
  centroid

  surface_type
  relief_type
  cover_type
  soil_type
  climate_type
  moisture_type

  feature_flags
  resource_type

  province_id
  region_id
  zone_id
  macroregion_id
  superregion_id
  continent_id
}
```

---

## 2.3 Runtime World State

Это текущее состояние партии.

Сюда входит:

```text
владелец клетки
контролёр клетки
освоение клетки
уровень освоения
население
разорение
дороги
мосты
города
армии
флоты
строительства
активные события
рынки
войны
дипломатия
```

Пример:

```gdscript
CellState {
  cell_id

  owner_polity_id
  controller_polity_id

  development_type
  development_level
  maturity
  damage
  devastation

  road_level
  irrigation_level
  has_bridge
  fortification_level

  rural_population
  rural_capacity

  worked_by_city_id
  state_flags
}
```

---

## 2.4 Cache

Кэш — это заранее посчитанные значения, чтобы игра не пересчитывала всё каждый кадр.

```gdscript
CellCache {
  cell_id

  food_potential
  pasture_potential
  forest_potential
  mining_potential
  settlement_potential

  current_food_output
  current_resource_output
  trade_value
  economic_value

  defense_value
  supply_limit
  attrition_risk
  movement_base_cost
}
```

Кэш пересчитывается только когда что-то изменилось:

```text
построили дорогу
сменили освоение
изменилась власть
появилось разорение
город подключил клетку
клетка попала в засуху
появился мост
изменилась инфраструктура
```

Главное правило оптимизации:

```text
Не пересчитывать всю карту, если изменилась одна клетка.
```

---

# 3. Карта как граф

Визуально карта выглядит как набор полигонов, но логически это граф:

```text
клетки = узлы
переходы между клетками = рёбра
```

Это фундамент для движения, снабжения, торговли, AI и войны.

```gdscript
MapGraph {
  cells[]
  transitions[]
  adjacency[cell_id] = transition_ids[]
}
```

Переход между клетками:

```gdscript
TransitionStatic {
  id
  from_cell_id
  to_cell_id

  distance_km
  border_length_km

  transition_type
  allowed_unit_types

  has_river_crossing
  has_bridge
  has_mountain_pass
  has_sea_lane
  has_road_connection

  base_land_cost
  base_sea_cost
}
```

Главная идея:

```text
Движение считается не по клетке, а по переходу между клетками.
```

Это решает проблему огромных клеток, гор, Сибири, пустынь и океанов.

---

# 4. Площадь клеток

Площадь надо считать и для суши, и для моря.

## 4.1 Сухопутная клетка

Площадь влияет на:

```text
потенциал населения
пашню
пастбища
лес
рудники
контроль территории
локальную экономику
```

Но площадь нельзя использовать линейно.

Правильнее:

```text
area_factor = sqrt(area_km2 / 1000)
area_factor = clamp(area_factor, 0.4, 4.0)
```

То есть большая клетка даёт больше потенциала, но не становится автоматически в 30 раз сильнее.

---

## 4.2 Морская клетка

Площадь морской клетки влияет на:

```text
рыболовный потенциал
масштаб морской зоны
сложность патрулирования
шанс встречи флотов
пиратство
штормовой риск
морское снабжение
торговые маршруты
```

Но для движения флота главное:

```text
distance_km между центрами морских клеток
```

Пример морской клетки:

```gdscript
SeaCellStatic {
  id
  area_km2
  area_factor
  centroid

  water_type
  depth_type
  sea_zone_id

  coast_adjacency
  port_access
  fish_potential
  storm_risk
  piracy_risk
  trade_route_value
  naval_supply_value
}
```

Типы морских клеток:

```text
coastal_sea       // прибрежное море
open_sea          // открытое море
deep_ocean        // глубокий океан
inland_sea        // внутреннее море
strait            // пролив
lake              // озеро
river_estuary     // устье реки
```

---

# 5. Движение юнитов

Юнит не должен просто «прыгать» из клетки в клетку.

У него должно быть два состояния:

```text
стоит в клетке
находится в пути между клетками
```

```gdscript
UnitState {
  id
  unit_type
  owner_id

  location_type // cell или transition

  current_cell_id

  from_cell_id
  to_cell_id
  transition_id
  travel_progress
  travel_cost_total
  travel_cost_remaining

  movement_points_per_turn
  current_movement_points

  status // idle, marching, fighting, retreating, sieging
}
```

Если переход стоит 12 очков движения, а у армии 4 очка движения за ход:

```text
Ход 1: 4 / 12
Ход 2: 8 / 12
Ход 3: 12 / 12 — армия прибыла
```

На карте это выглядит так:

```text
Ход 0:
[ Горная клетка A ]        [ Горная клетка B ]
        ⚔

Ход 1:
[ Горная клетка A ] ---⚔---- [ Горная клетка B ]
Прогресс: 4/12

Ход 2:
[ Горная клетка A ] ------⚔- [ Горная клетка B ]
Прогресс: 8/12

Ход 3:
[ Горная клетка A ] -------- [ Горная клетка B ]
                                      ⚔
```

---

# 6. Формулы движения

## 6.1 Суша

```text
land_cost =
  clamp(
    distance_km / 30
    × relief_modifier
    × cover_modifier
    × road_modifier
    × weather_modifier
    × state_modifier,
    0.5,
    12
  )
```

Пример для огромной горной клетки:

```text
distance_km = 120
base = 120 / 30 = 4
mountains_modifier = 2.2
forest_modifier = 1.3
winter_modifier = 1.25

transition_cost = 4 × 2.2 × 1.3 × 1.25 = 14.3
```

Можно ограничить:

```text
max_land_transition_cost = 12 или 16
```

---

## 6.2 Горы и перевалы

Горы не должны быть просто «дорогими равнинами».

У переходов должны быть типы:

```text
обычный горный переход
перевал
дорога через перевал
непроходимые высокие горы
```

Пример:

```text
Горы без перевала: transition_cost = 14
Горный перевал: transition_cost = 7
Дорога через перевал: transition_cost = 4
Высокие горы без прохода: transition_allowed = false
```

---

## 6.3 Море

```text
sea_cost =
  clamp(
    distance_km / 80
    × water_type_modifier
    × weather_modifier
    × navigation_modifier
    × current_modifier,
    0.3,
    15
  )
```

Пример океанского перехода:

```text
distance_km = 500
base = 500 / 80 = 6.25
deep_ocean_modifier = 1.3
storm_modifier = 1.4
early_navigation_modifier = 1.3

sea_transition_cost = 6.25 × 1.3 × 1.4 × 1.3 ≈ 14.8
```

Флот с 5 очками движения будет идти 3 хода.

---

# 7. Запечённая карта

Для производительности статичные визуальные слои карты правильно запекать картинками.

Но важно:

```text
Запекать можно визуал.
Нельзя запекать игровую логику.
```

Правильная схема:

```text
Картинка карты = внешний вид
Данные карты = настоящая игровая логика
```

---

## 7.1 Что можно запекать

```text
физическую карту
рельефную подложку
цвета биомов
тени гор
береговую линию
моря/океаны
пустыни
леса как общий визуал
снег/тундру
декоративные реки
пергаментный стиль
```

Файлы:

```text
terrain_base.png
relief_shadows.png
water_base.png
biome_texture.png
coastline_detail.png
```

---

## 7.2 Что нельзя запекать навсегда

```text
владение клетками
контроль во время войны
освоение клеток
построенные дороги
разорение
горящие города
движение юнитов
маршруты
выделение клетки
подсветку провинций
торговые пути
снабжение
режимы карты
```

Это должно рисоваться динамическими overlay-слоями.

---

## 7.3 Не одна огромная картинка, а тайлы

Плохой вариант:

```text
world_map_50000x25000.png
```

Проблемы:

```text
долго грузится
жрёт память
может упереться в лимит текстуры
плохо работает на слабых ПК
сложно делать разные масштабы
```

Правильный вариант:

```text
разбить карту на тайлы / чанки
```

Например:

```text
map_tiles/
  zoom_0/
    tile_0_0.png
    tile_1_0.png

  zoom_1/
    tile_0_0.png
    tile_1_0.png
    tile_2_0.png

  zoom_2/
    tile_0_0.png
    tile_1_0.png
    tile_2_0.png
    tile_3_0.png
```

Рекомендуемый размер тайлов:

```text
2048×2048
```

или:

```text
4096×4096
```

---

# 8. ID-карта для кликов

Если визуальная карта запечена картинкой, Godot сам не знает, где какая клетка.

Лучшее решение для MVP — невидимая ID-карта.

ID-карта — это специальная картинка, где каждая клетка закрашена уникальным цветом.

```text
клик мыши
↓
координата карты
↓
пиксель ID-карты
↓
цвет
↓
cell_id
```

Пример:

```text
клетка 1523 = RGB(0, 5, 243)
```

Файлы:

```text
visual_map_tiles/
  terrain_0_0.png

id_map_tiles/
  id_0_0.png
```

Плюсы:

```text
очень быстро
не нужны тысячи CollisionPolygon
легко определить клетку под мышью
хорошо работает с запечённой картой
```

Минусы:

```text
нужно аккуратно генерировать ID-карту
координаты visual map и id map должны идеально совпадать
```

Для проекта это оптимальное решение.

---

# 9. Визуальная архитектура карты в Godot

Не надо делать каждую клетку отдельной сценой.

Плохой вариант:

```text
20 000 клеток = 20 000 Node2D
каждый Node сам считает, сам рисует, сам ловит мышку
```

Правильный вариант:

```text
MapRenderer
  рисует карту тайлами
  отдельно рисует overlay
  отдельно рисует границы
  отдельно рисует юниты
  отдельно рисует маршруты
```

Структура:

```text
MapRoot
  TerrainTiles
  ReliefTiles
  WaterTiles
  StaticBorderLayer
  DynamicOverlayLayer
  RouteLayer
  CityMarkers
  UnitMarkers
  SelectionLayer
```

Скрипты:

```text
MapRenderer.gd
MapTileLoader.gd
MapSelectionController.gd
IdMapPicker.gd
OverlayRenderer.gd
RouteRenderer.gd
UnitRenderer.gd
CityMarkerRenderer.gd
```

---

# 10. Слои карты

Визуальные слои надо разделить.

```text
TerrainLayer
PoliticalLayer
DevelopmentLayer
InfrastructureLayer
TradeLayer
SupplyLayer
MilitaryLayer
CultureLayer
PopulationLayer
DebugLayer
```

Игрок может переключать режимы карты:

```text
физическая карта
политическая карта
освоение
население
торговля
снабжение
военная карта
культура
разорение
```

Каждый режим карты должен брать данные из кэша, а не пересчитывать мир.

---

# 11. Территориальная иерархия

География должна быть стабильной и не зависеть от государств.

```text
Cell
↓
Province
↓
Region
↓
Zone
↓
Macroregion
↓
Superregion
↓
Continent
↓
World
```

Пример:

```text
Клетка: Рыбацкое побережье Ла-Коруньи
Провинция: Ла-Корунья
Регион: Галисия
Зона: Северо-Западная Иберия
Макрорегион: Иберия
Суперрегион: Западная Европа
Континент: Европа
Мир: Земля
```

---

## 11.1 Cell

Клетка — минимальная физическая единица.

Хранит:

```text
рельеф
почву
покров
климат клетки
ресурс
освоение
дороги
сельское население
повреждение
```

---

## 11.2 Province

Провинция — локальная экономика вокруг центра.

Хранит:

```text
список клеток
главный город
локальную еду
локальное население
локальный порядок
гарнизон
локальный рынок
локальное разорение
```

---

## 11.3 Region

Регион — историко-географическая область.

Пример:

```text
Галисия
```

Хранит:

```text
базовую плотность населения
региональный культурный профиль
традиции
хозяйственный уклон
торговый уклон
климатический профиль региона
```

Важно:

```text
климат клетки = точный климат
климат региона = обобщённый профиль для генерации, событий и AI
```

---

## 11.4 Zone

Зона — связка соседних регионов.

Хранит:

```text
логистические связи
торговую ориентацию
военный театр
зону снабжения
общий природный характер
```

---

## 11.5 Macroregion

Макрорегион — большая цельная область.

Хранит:

```text
крупные торговые пути
морские выходы
главные барьеры
крупные рынки
геополитическую важность
```

---

## 11.6 Superregion

Суперрегион — огромный экономико-цивилизационный блок.

Хранит:

```text
скорость распространения институтов
уровень урбанизации
плотность торговли
уровень дорог
технологическую среду
```

---

## 11.7 Continent

Континент хранит:

```text
крупные события
эпидемии
континентальные миграции
межсуперрегиональную торговлю
климатические рамки
```

---

## 11.8 World

Мир хранит:

```text
год
эпоху
мировые цены
глобальный климат
открытие океанской навигации
глобальные события
```

---

# 12. Политика отдельно от географии

Нельзя смешивать регион и владельца региона.

```text
Галисия как регион = география
Кастилия контролирует Галисию = политика
```

Географический регион остаётся Галисией независимо от того, кто им владеет.

Для политического объекта лучше использовать нейтральный термин:

```text
Polity
```

```gdscript
PolityState {
  id
  name
  capital_city_id

  treasury
  manpower
  legitimacy
  stability

  owned_cell_ids
  controlled_cell_ids

  diplomacy_state
  laws
  institutions
}
```

Клетка хранит:

```text
owner_polity_id
controller_polity_id
```

---

# 13. Системы симуляции

Системы должны быть отдельными.

```text
TurnManager
MovementSystem
EconomySystem
PopulationSystem
CitySystem
DevelopmentSystem
LogisticsSystem
CombatSystem
SiegeSystem
TradeSystem
EventSystem
AISystem
```

---

## 13.1 TurnManager

Отвечает за порядок хода.

```gdscript
TurnManager {
  start_turn()
  collect_player_orders()
  collect_ai_orders()
  resolve_orders()
  process_world_tick()
  end_turn()
}
```

Пример порядка хода:

```text
1. Игрок отдаёт приказы
2. AI отдаёт приказы
3. Движение
4. Бои / столкновения
5. Осады
6. Производство еды и ресурсов
7. Население
8. Торговля
9. События
10. Обновление кэшей
11. UI получает результат
```

---

## 13.2 MovementSystem

Отвечает за движение.

```gdscript
MovementSystem {
  calculate_transition_cost(unit, transition)
  start_movement(unit, target_cell)
  advance_travel(unit)
  finish_travel(unit)
  cancel_travel(unit)
}
```

Работает с:

```text
MapGraph
TransitionStatic
UnitState
CellState
```

Не должен зависеть от анимации.

---

## 13.3 EconomySystem

Отвечает за производство.

```gdscript
EconomySystem {
  calculate_cell_output(cell)
  calculate_province_output(province)
  calculate_city_consumption(city)
  update_markets()
}
```

Главная логика:

```text
клетка производит сырьё
город перерабатывает сырьё
провинция собирает локальную экономику
торговля связывает рынки
```

---

## 13.4 PopulationSystem

Отвечает за население.

```gdscript
PopulationSystem {
  calculate_rural_capacity(cell)
  calculate_urban_capacity(city)
  process_growth()
  process_migration()
  process_losses()
}
```

Разделение:

```text
сельское население хранится в клетке
городское население хранится в городе
```

---

## 13.5 CitySystem

Отвечает за города.

```gdscript
CitySystem {
  update_city_capacity(city)
  update_food_demand(city)
  update_worked_cells(city)
  unlock_worked_cell_slots(city)
  process_buildings(city)
}
```

Город:

```gdscript
CityState {
  id
  name
  cell_id

  urban_population
  urban_capacity

  worked_cell_slots
  worked_cell_ids

  food_stock
  housing_level
  water_access
  market_level
  port_level
  health
  order
}
```

Пороги рабочих клеток:

```text
0–999      → 1 клетка
1 000+     → 2 клетки
5 000+     → 3 клетки
10 000+    → 4 клетки
20 000+    → 5 клеток
```

---

## 13.6 DevelopmentSystem

Отвечает за освоение клеток.

```gdscript
DevelopmentSystem {
  can_develop(cell, development_type)
  calculate_suitability(cell, development_type)
  start_development(cell, development_type)
  progress_development(cell)
  update_maturity(cell)
  process_overuse(cell)
}
```

Освоение — это не здание, а использование земли:

```text
пашня
пастбища
виноградники
оливковые рощи
лесозаготовка
рудный промысел
рыбацкие пристани
```

---

## 13.7 LogisticsSystem

Отвечает за снабжение.

```gdscript
LogisticsSystem {
  calculate_supply_network()
  calculate_unit_supply(unit)
  calculate_city_supply(city)
  calculate_route_capacity(route)
}
```

Учитывает:

```text
дороги
порты
реки
мосты
перевалы
склады
расстояние
контроль территории
морские зоны
```

Логистика может быть одной из главных фишек игры.

---

## 13.8 CombatSystem

Отвечает за бои.

```gdscript
CombatSystem {
  detect_battles()
  calculate_combat_width()
  calculate_defense_bonus()
  resolve_battle()
  apply_losses()
  handle_retreat()
}
```

Берёт данные из:

```text
рельефа клетки
перехода
статуса юнита
снабжения
погоды
```

Юнит в пути должен сражаться хуже, чем укреплённый юнит в клетке.

---

## 13.9 SiegeSystem

Отвечает за осады.

```gdscript
SiegeSystem {
  start_siege(city_or_fort)
  update_siege_progress()
  calculate_supply_pressure()
  resolve_assault()
}
```

Осады привязаны к:

```text
городам
крепостям
портам
горным проходам
ключевым дорогам
```

---

## 13.10 TradeSystem

Отвечает за торговлю.

```gdscript
TradeSystem {
  build_trade_routes()
  calculate_trade_value()
  update_prices()
  move_goods()
}
```

Торговля не должна быть просто деньгами из воздуха.

Правильная логика:

```text
клетка даёт сырьё
город перерабатывает
провинция собирает
маршрут перевозит
рынок создаёт цену
```

---

## 13.11 EventSystem

Отвечает за события.

```gdscript
EventSystem {
  check_triggers()
  create_event()
  apply_event_effect()
}
```

События должны иметь скоуп:

```text
cell
province
region
zone
macroregion
superregion
continent
world
```

Примеры:

```text
засуха — регион / зона
эпидемия — провинция / регион / континент
морская буря — морская зона
кризис торговли — макрорегион / суперрегион
```

---

## 13.12 AISystem

AI должен работать не с картинкой, а с кэшированными значениями.

```gdscript
AISystem {
  evaluate_cells()
  evaluate_provinces()
  evaluate_threats()
  choose_development()
  plan_movement()
  plan_war()
  plan_trade()
}
```

AI нужны кэши:

```text
economic_value
military_value
strategic_value
trade_value
supply_value
threat_value
```

---

# 14. UI-архитектура

UI не должен сам считать экономику.

UI должен получать готовые ViewModel:

```text
CellViewModel
ProvinceViewModel
RegionViewModel
ZoneViewModel
CityViewModel
UnitViewModel
```

Пример:

```gdscript
CellViewModel {
  title
  subtitle
  terrain_description
  current_development
  output_lines[]
  warnings[]
  possible_actions[]
}
```

UI должен спрашивать:

```text
дай мне карточку клетки
```

А не:

```text
дай мне все сырые данные, я сам посчитаю
```

---

# 15. Карточки уровней

Для каждого уровня нужен свой инспектор.

```text
CellCard
ProvinceCard
RegionCard
ZoneCard
MacroregionCard
SuperregionCard
ContinentCard
WorldPanel
```

## CellCard

Показывает:

```text
рельеф
почву
покров
ресурс
освоение
выход
движение
оборону
состояние
```

## ProvinceCard

Показывает:

```text
город
клетки
еду
население
порядок
доход
гарнизон
```

## RegionCard

Показывает:

```text
культуру
базовую плотность
традиции
хозяйственный уклон
региональные товары
```

## ZoneCard

Показывает:

```text
логистику
торговые связи
военный театр
морскую/сухопутную связность
```

## MacroregionCard

Показывает:

```text
крупные рынки
морские выходы
барьеры
ключевые маршруты
```

## SuperregionCard

Показывает:

```text
урбанизацию
технологическую среду
институты
крупную торговлю
```

## ContinentCard

Показывает:

```text
континентальные события
эпидемии
миграции
крупные торговые связи
```

## WorldPanel

Показывает:

```text
год
эпоху
мировые цены
глобальные события
технологические рубежи
```

---

# 16. Event Bus

Системы не должны напрямую дергать друг друга хаотично.

Нужен EventBus.

Примеры событий:

```text
UnitArrived
CellDevelopmentChanged
CityPopulationChanged
ProvinceDevastated
BattleStarted
BattleFinished
TradeRouteBroken
RoadBuilt
CityStarving
```

Пример работы:

```text
MovementSystem завершил движение
↓
создал UnitArrivedEvent
↓
CombatSystem проверил столкновение
↓
SupplySystem обновил снабжение
↓
UI обновил отображение
```

---

# 17. Dirty Flags

Нельзя пересчитывать всё всегда.

У объектов должны быть флаги изменений:

```text
cell.dirty_economy = true
cell.dirty_movement = true
province.dirty_population = true
region.dirty_summary = true
```

Пример:

```text
построили дорогу в клетке
↓
dirty_movement = true
dirty_trade = true
dirty_supply = true
```

После этого пересчитываются только нужные кэши.

---

# 18. Save / Load

Сохранение не должно хранить всю статическую карту.

Плохой вариант:

```text
сохранять все полигоны клеток в каждом сейве
```

Правильный вариант:

```text
статическая карта лежит в игре
сейв хранит только изменения
```

```gdscript
SaveGame {
  version
  current_turn
  world_state_id

  cell_states[]
  city_states[]
  unit_states[]
  polity_states[]
  active_events[]
  market_states[]
}
```

Клетка в сейве:

```gdscript
CellStateSave {
  cell_id
  owner_id
  controller_id
  development_type
  development_level
  maturity
  damage
  road_level
  rural_population
}
```

Обязательно хранить:

```text
version
```

Потому что формат сейвов будет меняться.

---

# 19. Папки проекта

Пример структуры:

```text
res://
  src/
    core/
      data/
        cell_static.gd
        cell_state.gd
        city_state.gd
        unit_state.gd
        polity_state.gd

      world/
        world_state.gd
        world_static.gd
        map_graph.gd
        transition.gd

      systems/
        turn_manager.gd
        movement_system.gd
        economy_system.gd
        population_system.gd
        city_system.gd
        development_system.gd
        logistics_system.gd
        combat_system.gd
        siege_system.gd
        trade_system.gd
        event_system.gd
        ai_system.gd

      balance/
        balance_database.gd
        modifier_resolver.gd

      save/
        save_manager.gd
        save_serializer.gd
        save_migration.gd

    map/
      map_renderer.gd
      map_tile_loader.gd
      map_selection_controller.gd
      id_map_picker.gd
      map_overlay_manager.gd
      route_renderer.gd
      unit_renderer.gd
      city_marker_renderer.gd

    ui/
      cards/
        cell_card.gd
        province_card.gd
        region_card.gd
        zone_card.gd
        macroregion_card.gd
        superregion_card.gd
        continent_card.gd
        world_panel.gd

      viewmodels/
        cell_view_model.gd
        province_view_model.gd
        city_view_model.gd
        unit_view_model.gd

    ai/
      ai_planner.gd
      ai_economy.gd
      ai_military.gd
      ai_development.gd

    tools/
      map_importer/
      validators/
      debug_overlays/

  content/
    balance/
      terrain_balance.json
      cover_balance.json
      soil_balance.json
      climate_balance.json
      development_balance.json
      resource_balance.json
      movement_balance.json
      trade_balance.json
      supply_balance.json

    world/
      cells.json
      transitions.json
      provinces.json
      regions.json
      zones.json
      macroregions.json
      superregions.json
      continents.json
      cities.json
      sea_zones.json

    map_tiles/
      terrain/
      relief/
      water/
      id_map/

  assets/
    textures/
    icons/
    ui/
    fonts/
```

---

# 20. Autoload / Singleton в Godot

Можно использовать несколько глобальных сервисов:

```text
Game
BalanceDB
World
EventBus
SaveManager
UIRouter
```

Не надо делать 20 Singleton-ов.

## Game

```text
текущая партия
состояние игры
ссылка на WorldState
ссылка на TurnManager
```

## BalanceDB

```text
загружает таблицы баланса
даёт модификаторы системам
```

## World

```text
доступ к статической карте
доступ к runtime-состоянию
```

## EventBus

```text
сообщения между системами
```

## SaveManager

```text
сохранение
загрузка
версии сейвов
```

---

# 21. Импорт карты и генерация

Карту не надо генерировать тяжело при запуске игры.

Нужен отдельный pipeline:

```text
GIS / Natural Earth / custom polygons
↓
очистка геометрии
↓
разделение на клетки
↓
расчёт площадей
↓
расчёт центроидов
↓
расчёт соседств
↓
расчёт переходов
↓
назначение провинций/регионов
↓
назначение природы
↓
генерация visual tiles
↓
генерация id-map tiles
↓
экспорт StaticWorldData
↓
Godot загружает готовый результат
```

Файлы результата:

```text
cells.json
transitions.json
provinces.json
regions.json
cities.json
terrain_tiles/
id_map_tiles/
```

---

# 22. Производительность

Проект будет нормально работать даже при большом количестве клеток, если соблюдать правила.

Можно:

```text
карта тайлами
id-map для кликов
клетки как данные, не как сцены
кэшировать экономику
кэшировать значения для AI
pathfinding только при приказе
экономику считать раз в ход
UI брать ViewModel
```

Нельзя:

```text
каждую клетку делать Node2D
каждый кадр пересчитывать доход всех клеток
каждый кадр пересчитывать pathfinding всех армий
UI считать формулы самостоятельно
хранить все полигоны в сейве
грузить одну огромную PNG всего мира
```

---

# 23. Минимальная архитектура первого прототипа

Первый прототип должен быть очень узким.

Нужно сделать:

```text
1. StaticWorldData
2. CellStatic + CellState
3. TransitionStatic
4. MapGraph
5. Запечённые terrain tiles
6. ID-map для кликов
7. MapRenderer
8. MapSelectionController
9. UnitState
10. MovementSystem
11. TurnManager
12. CellCard
```

Цель первого прототипа:

```text
карта отображается
клетки кликаются
карточка клетки открывается
армия стоит в клетке
армия может идти в соседнюю клетку
если переход длинный — армия идёт несколько ходов
```

Это фундамент проекта.

---

# 24. Второй этап

Добавить:

```text
Province
City
worked cells
population thresholds
DevelopmentSystem
EconomySystem
PopulationSystem
```

Цель:

```text
город Ла-Корунья
5 внешних рабочих клеток
порог населения открывает клетки
пашня даёт еду
рыбацкая клетка даёт рыбу
город потребляет еду
население растёт или падает
```

---

# 25. Третий этап

Добавить:

```text
дороги
снабжение
боёвку
осады
разорение
AI
события
```

Цель:

```text
дороги реально ускоряют армии
горы реально мешают
порт реально помогает снабжению
война портит клетки
AI понимает стратегические точки
```

---

# 26. Самая правильная последовательность разработки

```text
Этап 1 — карта и клетки
Этап 2 — граф переходов и движение
Этап 3 — город и рабочие клетки
Этап 4 — производство еды и сырья
Этап 5 — население
Этап 6 — дороги и логистика
Этап 7 — война
Этап 8 — торговля
Этап 9 — AI
Этап 10 — события
Этап 11 — UI-полировка
Этап 12 — демо
```

Не надо начинать с культуры, религии, дипломатии и мировой экономики, пока юнит не может нормально пройти из одной клетки в другую.

---

# 27. Главные ошибки, которых нельзя допустить

```text
1. Делать каждую клетку сценой с логикой.
2. Хранить все модификаторы прямо в клетке.
3. Считать экономику в UI.
4. Смешивать географию и политические владения.
5. Делать движение как “одна клетка = один шаг”.
6. Делать всю игру в одном GameManager.
7. Генерировать всю карту при каждом запуске.
8. Не иметь кэша.
9. Не иметь версий сейвов.
10. Начинать с полной карты мира и всех механик.
11. Запекать динамические игровые слои в статичную картинку.
12. Грузить одну огромную карту вместо тайлов.
13. Делать UI источником игровых формул.
14. Давать AI сырые данные вместо кэшированной оценки.
```

---

# 28. Итоговая архитектурная формула

```text
Статичная красота = запечённые тайлы
Игровая логика = JSON / граф / состояние
Динамика = overlay-слои
Движение = переходы между клетками
Юнит = клетка или маршрут
Экономика = системы + кэш
UI = ViewModel, а не формулы
AI = оценки, а не пиксели
Сейв = только изменения
```

Самая важная мысль:

```text
Клетка — физическая земля.
Провинция — локальная экономика.
Регион — историко-географический характер.
Город — население, переработка и управление.
Переход — движение и логистика.
Юнит — состояние на карте или в пути.
Система — считает правила.
UI — показывает и даёт команды.
```

Если это разделить правильно, проект можно будет расширять годами.

Если смешать всё в одном месте, проект начнёт ломаться уже на этапе добавления дороги через горный перевал.

---

# 29. Короткий вывод

Да, эта архитектура рассчитана как максимально оптимизированная для твоей игры:

```text
запечённая физическая карта
тайлы вместо огромной картинки
id-map для кликов
данные клеток отдельно
граф переходов отдельно
runtime-состояние отдельно
кэш отдельно
UI отдельно
симуляция отдельно
AI отдельно
сейвы отдельно
```

Это не самая простая архитектура, но она правильная для проекта с большой картой, множеством клеток, глубокими системами и перспективой дальнейшего расширения.
