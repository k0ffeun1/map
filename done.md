# Что сделано (Global Strategy Prototype)

## Базовая карта
- Тайловая карта мира (slippy map, схема z/x/y), стриминг по видимым тайлам,
  LOD-уровни `MIN_Z..MAX_Z` (сейчас `MAX_Z = 7`).
- Базовый слой — онлайн-тайлы реальной Земли: [OnlineTileProvider.gd](scripts/OnlineTileProvider.gd)
  (сервер ESRI, сейчас стиль `World_Physical_Map` — стилизованный атлас без подписей).
- Камера: [CameraController.gd](scripts/CameraController.gd)
  - WASD / стрелки — плавное перемещение (lerp);
  - колесо мыши — зум к курсору;
  - ПКМ / СКМ (зажать) — перетаскивание карты;
  - `R` — сброс (вписать всю карту в экран);
  - `Esc` — выход;
  - ограничение камеры по границам карты.
- Движок стриминга/слоёв: [TileMapViewer.gd](scripts/TileMapViewer.gd)
  — рендерит несколько слоёв поверх друг друга, переключение клавишами `1`–`6`.

## Слои (клавиши 1–6)
1. **Спутник/атлас** — реальная карта Земли (онлайн).
2. **Политический** — оверлей-заглушка (процедурный шум по регионам).
3. **Экономический** — оверлей-заглушка (процедурный тепловой градиент).
4. **Религиозный** — оверлей-заглушка (процедурный шум по регионам).
5. **Клетки** — глобальный слой клеток-провинций по всему миру
   ([CellTileProvider.gd](scripts/CellTileProvider.gd)):
   - только границы (скруглённые квадраты линиями), без цветной заливки суши —
     заливку убрали по просьбе (было промежуточным цветным шумом-заглушкой);
   - реальная береговая линия — маска суши строится из данных высот
     (Terrarium elevation tiles), а не из спутниковой картинки;
   - маска сглажена majority-фильтром 3×3, чтобы убрать шумовые "дыры"
     в низинных регионах (Нидерланды, дельта По, дельта Нила, низины у Каспия) —
     порог "суша/море" сдвинут до `-8 м`, чтобы польдеры и т.п. не резались как море;
   - клетки не рисуются за полярными порогами (`NORTH_CUTOFF_Y`/
     `SOUTH_CUTOFF_Y`, ~76°N/~58°S) — убрана вся Антарктида и дальняя
     необитаемая Арктика (Гренландия выше поселений, Шпицберген, арктические
     острова); пороги подобраны по скриншоту, не облётаны по всему миру.
6. **Моря** — контуры морей/заливов/проливов/звуков/каналов/лагун/фьордов
   ([SeaBorderTileProvider.gd](scripts/SeaBorderTileProvider.gd)), только линии,
   без заливки:
   - геоданные — Natural Earth `ne_10m_geography_marine_polys` (общественное
     достояние), 301 объект после фильтрации по типу;
   - офлайн-препроцессинг (не в Godot, один раз):
     [build_sea_borders.py](scripts/tools/build_sea_borders.py) — проецирует
     полигоны в Web Mercator (та же система координат `WORLD_PX`, что и
     остальные слои), упрощает Douglas-Peucker (117k → ~35k точек), сохраняет
     в [assets/sea_borders.json](assets/sea_borders.json) (672 КБ);
   - рендер синхронный, без сети: данные грузятся один раз в память при
     старте, каждый тайл клипует и рисует линии из колец, чьи bbox
     пересекают тайл (ручная растеризация отрезков с антиалиасингом по краю,
     без встроенного polyline-API у `Image`);
   - слой НЕ участвует в `TilePreloader` (не нужно — данные уже локальные).

## Загрузка тайлов
- Кэш на диске: `user://tilecache` (карта) и `user://elevcache` (высоты) —
  повторный запуск игры использует уже скачанное без сети.
- **Bundle-кэш в проекте**: `assets/tiles_bundle/tilecache` и
  `assets/tiles_bundle/elevcache` — то, что "запечено" прямо в проект/игру
  (проверяется провайдерами ПЕРВЫМ, до сети и до `user://`).
  Перенесён ПОЛНЫЙ мир (весь `MAX_Z`): 22 719 файлов (~166 МБ) карты +
  21 795 файлов (~996 МБ) высот — предзагрузка завершена, перенос из
  `user://` сделан вручную (см. "На будущее" — процесс ещё не автоматизирован).
  Билд/установщик с этого момента полностью офлайн.
- Фоновая предзагрузка всех тайлов мира при старте:
  [TilePreloader.gd](scripts/TilePreloader.gd) — качает весь мир до `MAX_Z`
  для карты и высот параллельно (пул HTTPRequest), пишет прогресс внизу экрана.
  Использовать игру можно и во время предзагрузки.

## Производительность (эта сессия)
Карта заметно фризила/мигала при зуме. Причины и фиксы:
- **LOD дребезжал во время анимации зума** — считался от промежуточного
  сглаженного `zoom` каждый кадр, а не от целевого. Теперь
  [TileMapViewer.gd](scripts/TileMapViewer.gd) берёт `camera.get_target_zoom()`
  (новый метод в [CameraController.gd](scripts/CameraController.gd)) и
  переключает уровень один раз за жест, с гистерезисом `LOD_HYSTERESIS`.
- **Старый LOD выгружался мгновенно, до готовности нового** — при смене
  уровня видимого слоя тайлы старого LOD теперь держатся, пока не готовы
  все нужные тайлы нового (см. `_process` в `TileMapViewer.gd`) — мигание
  пустотой при зуме исчезло.
- **Декодирование тайлов и рендер клеток блокировали кадр** —
  [OnlineTileProvider.gd](scripts/OnlineTileProvider.gd) (чтение файла +
  JPEG/PNG-декодирование) и [CellTileProvider.gd](scripts/CellTileProvider.gd)
  (маска суши + отрисовка клеток, ~600 тыс. итераций на тайл) теперь
  выполняются в `WorkerThreadPool`, главный поток только создаёт текстуру из
  готового `Image`. Оба провайдера дожидаются своих фоновых задач в
  `_exit_tree()` — иначе поток мог дёрнуть уже уничтоженный объект при выходе.
- **`TilePreloader` фризил старт и отжирал сеть** — теперь стартует с
  задержкой `START_DELAY = 10s`, качает меньшим пулом (3 вместо 10) и
  сканирует уже скачанный кэш порциями (`SCAN_BUDGET_PER_FRAME`), а не всё за
  один кадр.

## Слой подписей морей
[SeaLabelsLayer.gd](scripts/SeaLabelsLayer.gd) + [SeaLabelNode.gd](scripts/SeaLabelNode.gd) —
не тайловый слой (подписей мало, ~300), создаются один раз, показываются
вместе со слоем "Моря". Имя/координата центроида берутся из
`SeaBorderTileProvider.get_labels()` (area-weighted центроид самого крупного
кольца объекта). Масштаб каждой подписи выставляется каждый кадр
`= 1/zoom камеры`, чтобы текст был константного размера на экране при любом
зуме, а не съезжал/не раздувался.

## Убраны Антарктида и Крайний Север — везде, не только у клеток
Раньше обрезка по широте (`NORTH_CUTOFF_Y`/`SOUTH_CUTOFF_Y`, ~76°N/~58°S)
работала только в слое "Клетки". Теперь:
- границы ВСЕЙ карты (`camera.set_map_bounds`) обрезаны по этим широтам —
  камера физически не может провернуться туда ни на одном слое;
- **дополнительно** поверх всех слоёв рисуется закрашенный
  прямоугольник-маска (`_add_polar_mask` в `TileMapViewer.gd`) — при сильном
  отдалении ("вписать всю карту", `R`) несовпадение соотношения сторон окна и
  обрезанной карты давало полоску полюса по краю экрана даже при
  ограниченной камере; маска гарантированно перекрывает её независимо от
  того, что нарисовано снизу.

## Неровные (Voronoi) клетки суши — слой "Клетки суши (неровные)" (клавиша 7)
Офлайн-препроцессинг (не в Godot, один раз, как `build_sea_borders.py`):
[build_land_cells.py](scripts/tools/build_land_cells.py) →
[assets/land_cells.json](assets/land_cells.json), рендерится
[IrregularCellProvider.gd](scripts/IrregularCellProvider.gd) (заливка +
тонкая граница, рендер в `WorkerThreadPool`, как остальные тяжёлые провайдеры).

- **Данные** — Natural Earth `ne_10m_land` (полигоны суши), 6837 отдельных
  кусков после разбора `MultiPolygon` (остров/материк = один кусок).
- **Число клеток на кусок суши** зависит от площади (км², считается по
  исходным lon/lat ДО проекции):
  - < 300 км² — клеток нет (слишком мелкий остров, не показываем);
  - 300–3000 км² — 1 клетка (весь остров);
  - 3000–15000 км² — 2 клетки;
  - 15000–50000 км² — 3–4 (линейная интерполяция);
  - > 50000 км² (материки, крупные острова) — `площадь / TARGET_CELL_AREA_KM2`
    (сейчас `10000`, даёт плавную стыковку на границе 50000 км²).
- **Плотность по регионам** (`SPARSE_ZONES`/`BOOST_ZONES` в скрипте) — не
  общий множитель, а прямоугольники lon/lat с локальным весом, применяются
  при расстановке точек-семян Voronoi (не только к общему числу клеток):
  - реже (крупнее клетки): Сибирь, Сахара, Амазония, внутренняя Австралия,
    тайга/тундра Канады, Патагония, Конго, Аравия (Руб-эль-Хали),
    Центр. Азия/Гоби, Тибет, Калахари/Намиб;
  - гуще (мельче клетки): Европа, вес `5.0` относительно базового `1.0`
    (нормализуется по `GLOBAL_MAX_WEIGHT`, иначе вес ≥1 всегда проходил бы
    проверку и буст не работал бы).
- **Гренландия — особый случай**: не обычный остров (обитаемо только
  побережье), клетки строятся ТОЛЬКО в прибрежной полосе (`poly.boundary.buffer`),
  центр — ледник без клеток вообще (см. `GREENLAND_BBOX`/`GREENLAND_COASTAL_BUFFER_KM`).
- **Волнистые (изрезанные) границы** — не Voronoi-прямые: каждое РЕБРО
  Voronoi (не весь контур клетки) сглажено рекурсивным смещением середины
  отрезка (как рисуют реки в генераторах карт), см. `_displace_segment`.
  Кэшировано по ключу (индекс вершины A, индекс вершины Б) — у двух соседних
  клеток одно и то же ребро получает ИДЕНТИЧНУЮ волнистую линию (без щелей на
  стыке). Пробовали сначала сглаживание Чайкина (скругление углов
  многоугольника целиком) — оно смягчает только вершины, рёбра между ними
  остаются идеально прямыми; это не то, что нужно ("изрезанные линии, как
  реки"), поэтому заменили на подход с общим кэшем волнистых рёбер.
- **Меркатор-коррекция расстояния между семенами** — без неё север
  (Сибирь/Канада) искажён проекцией и получал систематически меньше клеток,
  чем должен был (расстояние между точками считалось в "мировых пикселях"
  без поправки на то, что 1 км на экваторе ≠ 1 км у полюса в Меркаторе).
- **Проверка на разрыв по антимеридиану** изначально ошибочно выбрасывала
  Афро-Евразию целиком (bbox шире половины мира — но это НЕ разрыв, а просто
  очень широкий континент от Португалии до Чукотки); исправлено на проверку
  скачка между СОСЕДНИМИ точками кольца, а не общей ширины bbox.
- **Пространственная сетка для min-distance проверки** между семенами —
  наивная O(n²) проверка (сравнение с каждой уже поставленной точкой) заняла
  бы часы для материков с тысячами семян; заменена на бакет-сетку с
  окном 3×3 (или шире у полюсов, где Меркатор-масштаб больше).
- Итог на момент предыдущей генерации: **15 061 клетка**, ~1.23 млн точек
  (файл `land_cells.json` ~23 МБ — заметно вырос из-за возросшей плотности и
  изрезанности, разово влияет на время загрузки при старте, см. TODO).

## Волнистая береговая линия (эта сессия)
Запрос пользователя по скриншотам: часть побережий выглядела "прямой рваной"
(реальные точки Natural Earth без сглаживания) — внутренние границы между
клетками уже были волнистые, а сама береговая линия нет.
- **Береговая линия теперь тоже волнистая**: в
  [build_land_cells.py](scripts/tools/build_land_cells.py) добавлена
  `_wavify_geom`/`_wavify_ring` — тот же приём рекурсивного смещения
  середины отрезка (`_displace_segment`), что и у внутренних Voronoi-рёбер,
  но применяется ОДИН РАЗ ко всему контуру суши (внешнее кольцо + дыры)
  сразу после `simplify(1.5)`, ДО разбиения на клетки. Слабее внутренних
  границ (`COAST_JAGGED_DEPTH=3`/`COAST_JAGGED_AMPLITUDE=0.16` против
  `4`/`0.28` у внутренних), чтобы не взорвать число точек на материках.
  Старое сглаживание Чайкина (`chaikin_smooth_closed`) и `_add_cell`
  (с `rdp`-упрощением поверх волнистого контура, что сводило волну обратно
  к прямым линиям) убраны — везде используется `_add_wavy_cell` без
  повторного упрощения.
- Итог новой генерации: **14 497 клеток**, ~1.37 млн точек контуров клеток
  (файл `land_cells.json` ~26 МБ).
- **Пробовали и откатили**: закрашивать мелкие острова (< 300 км²) сплошным
  цветом моря вместо прозрачной клетки (чтобы не просвечивал спутниковый
  слой) — пользователь попросил НЕ делать так, потому что позже спутниковый
  слой всё равно уберут и останутся только клетки, так что временный
  "мусор" от мелких островов не имеет смысла чинить сейчас. Код (`erase_cells`
  в JSON, `ERASE_COLOR`/рендер в `IrregularCellProvider.gd`) добавлялся и
  удалён обратно в эту же сессию.
- **Баг волнистого берега нашёлся и исправлен той же сессией**: смещение
  середины отрезка было СИММЕТРИЧНЫМ (в обе стороны), поэтому контур клетки
  местами уходил внутрь настоящей суши — снаружи клетки оставался виден
  кусок настоящей земли спутникового слоя (дыра). Исправлено —
  `_displace_segment_outward`/`_ring_signed_area` смещают береговую линию
  ТОЛЬКО наружу (в море), контур клетки может только расти, никогда не
  теряет исходную сушу.

## Мелкие острова у берега материка, плотность регионов, Гренландия (эта сессия)
- **Мелкие "прилипшие" острова убираются, а не мержатся**: острова тира
  "1 клетка" (300..3000 км²) в пределах `NEARSHORE_KM` (12 км) от куска суши
  крупнее `NEARSHORE_MAINLAND_KM2` (50 000 км²) теперь НЕ получают клетку
  вообще (`_is_nearshore_of_mainland`, STRtree-индекс материков
  `_build_mainland_index`) — раньше давало визуальный хаос (куча
  перекрывающихся волнистых мелких клеток рядом с берегом материка, видно
  в дельте Амазонки и у восточного побережья США). Есть список исключений
  `ISLAND_EXCEPTION_ZONES` (Карибский бассейн, Япония, Корея) — там мелкие
  острова рядом с материком/друг с другом остаются отдельными клетками
  (настоящие архипелаги), список расширяемый.
- **Плотность регионов**: добавлены `BOOST_ZONES` — Карибский бассейн,
  Япония, Корея, Нижние земли (Нидерланды/Бельгия — гуще, чем остальная
  Европа). Патагония убрана из `SPARSE_ZONES` (была лишней/сломанной на
  вид) — обычная плотность.
- **Гренландия ослаблена**: `GREENLAND_COASTAL_BUFFER_KM` 180→110, своя
  целевая площадь клетки `GREENLAND_TARGET_CELL_AREA_KM2=45000` (было
  пропорционально общей `TARGET_CELL_AREA_KM2=10000` острова — слишком
  много клеток в глубину узкой полосы) — теперь в среднем ~1-2 клетки
  вглубь от берега. Плюс `GREENLAND_NORTH_CUTOFF_LAT=72.0` — севернее клеток
  вообще нет (практически необитаемо).
- Итог: **15 649 клеток**, ~1.47 млн точек (файл `land_cells.json` ~27.6 МБ).

## Правки по скриншотам с красными квадратами (эта сессия)
- **Баг: Исландия/Баффинова Земля/Элсмир/Виктория/Девон получали "гренландский"
  режим** (только берег + отсечка севера) из-за слишком широкого порога
  `area > 100000` км² в проверке `is_arctic_island` — все они выше этого
  порога и центроид у всех попадает в `GREENLAND_BBOX`. Отсюда: Исландия
  одной большой клеткой, и большие куски суши на севере Канады вообще без
  клеток (их "гренландский" центр оказался ВЫШЕ `GREENLAND_NORTH_CUTOFF_LAT`,
  а прибрежная полоса у них не подразумевалась). Порог поднят до 1.5 млн
  км² (реальная Гренландия ~2.17 млн, следующий по размеру арктический
  остров Баффинова Земля ~507 тыс) — теперь ТОЛЬКО настоящая Гренландия
  получает урезанный режим, остальные — обычный Voronoi по всей площади.
- **`MIN_ISLAND_AREA_KM2` снижен с 300 до 15 км²** — при 300 пропадали
  вообще (без клетки) многие настоящие именованные острова (Средиземноморье,
  Дания, Британия/Ирландия, Фареры, часть Карибов).
- **`NEARSHORE_KM` уменьшен с 12 до 5 км`**, список `ISLAND_EXCEPTION_ZONES`
  расширен (добавлены Филиппины, Средиземноморье, Британия/Ирландия/Бретань,
  Дания/Фареры/промежуток Исландия-Британия) — при 12 км и коротком списке
  исключений под "удалить как мусор у берега" ошибочно попадали настоящие
  прибрежные острова (о. Уайт, Англси и т.п.), не только наносные островки
  дельт/заливов.
- **Филиппины "наезжали" друг на друга**: волнистость берега смещает контур
  только наружу (см. предыдущий фикс дыр), но БЕЗ ограничения по абсолютной
  величине — в тесном архипелаге соседние ОТДЕЛЬНЫЕ острова могли распухнуть
  навстречу друг другу через узкий пролив и визуально перекрыться. Добавлен
  `MAX_COAST_BULGE_PX=0.5` (~2.4 км на экваторе) — абсолютный потолок
  смещения поверх относительной доли от длины сегмента.
- Итог: **16 368 клеток**, ~1.47 млн точек (файл `land_cells.json` ~27.6 МБ).

## Известные починенные баги
- WASD не работал на русской раскладке — исправлено (input actions переведены
  на `physical_keycode`).
- Процедурный физический слой (`PhysicalTileProvider`) — раньше давал "шум"
  вместо материков из-за неверной частоты `FastNoiseLite` — исправлено
  (сейчас этот слой не используется как основной, но код в проекте остался,
  используется как маска для оверлеев-заглушек 2/3/4).
- Ошибка компиляции в `TilePreloader.gd` (`Cannot use subscript operator on
  a base of type "null"`) — исправлена явной типизацией `-> Variant`.
- Случайно сдвинутый узел `Ocean` (фон) в сцене — возвращён на исходные anchors.

## Континенты, суша/море, реки, озёра (эта сессия)

### Слой "Континенты" (клавиша `0`)
Три итерации, пока не нашли правильный подход:
1. Красили по политическому атрибуту `CONTINENT` у стран
   (`ne_10m_admin_0_countries`) — оказалось неверно: вся Россия одной
   страной попадала в "Европа" (включая Дальний Восток), Французская
   Гвиана (реально в Южной Америке) — тоже в "Европа" вместе с Францией.
2. Разрезали реальную береговую линию (`ne_10m_land`) вручную нарисованными
   прямоугольниками (Урал/Кавказ, Суэц, Панама) — по смыслу верно, но
   пользователь увидел ПРЯМЫЕ швы прямоугольников поверх карты (искусственные
   линии через Казахстан/Каспий и Синай/Израиль) — визуально неприемлемо.
3. **Итог** — готовый картографический датасет **Esri "World Continents"**
   (живой feature layer ArcGIS Living Atlas, сохранён в
   `scripts/tools/_work/world_continents_esri.geojson`) — реальные полигоны
   континентов от картографов, без самодельной геометрии. Esri разделяет
   Australia/Oceania на два — объединены в один индекс `OCEANIA` (6 континентов
   всего). См. [build_continents.py](scripts/tools/build_continents.py) →
   `assets/continents.json`.
- Рендер: живой scan-line (`IrregularCellProvider.gd`) тормозил на гигантских
  полигонах (Евразия, ~5900 точек контура) — пользователь перевёл рендер
  континентов на **запечённые офлайн PNG-тайлы**
  ([bake_continents_tiles.py](scripts/tools/bake_continents_tiles.py),
  [BakedTileProvider.gd](scripts/BakedTileProvider.gd), 2193 файла в
  `assets/tiles_bundle/continents_baked/`); `continents.json` остаётся
  источником правды для игровой логики (клик, принадлежность континенту),
  картинки этого не заменяют.

### Главный слой "Суша/Море" (клавиша `-`)
Новый фундаментальный слой — реальная береговая линия (`ne_10m_land`) минус
озёра (`ne_10m_lakes`), независимый от остальных слоёв (свой пайплайн, не
переиспользует `land_cells.json`/`build_land_cells.py`). Задуман как
источник истины для будущих игровых систем (клик "суша/море", генерация
провинций/клеток, движение юнитов), не просто картинка. См.
[build_land_sea.py](scripts/tools/build_land_sea.py) → `assets/land_sea.json`.
- Живой рендер занимает **~15 минут** — слишком медленно, нужна такая же
  запечка в PNG, как у континентов (обсудили план, **не сделано** — ждём
  решения пользователя, предложено написать отдельный
  `bake_land_sea_tiles.py`, переиспользуя уже существующий `BakedTileProvider.gd`,
  не трогая `bake_continents_tiles.py`).

### Слой "Реки" (клавиша `=`)
[build_rivers.py](scripts/tools/build_rivers.py) → `assets/rivers.json`,
рендерится [RiverTileProvider.gd](scripts/RiverTileProvider.gd) (полилинии,
не полигоны). Источник — Natural Earth `ne_10m_rivers_lake_centerlines`, три
уровня значимости по `scalerank` (`TIER_MAX_RANK = [2, 5, 8]`), разная
толщина/цвет по уровню:
- крупные (0-2) — 112 отрезков (Нил, Амазонка, Янцзы, Дунай, Миссисипи,
  Конго, Обь и т.п.);
- средние (3-5) — 464 отрезка (Рейн, Сена, Тигр, Волга, Днепр, Эбро, Тахо
  и т.п.);
- мелкие (6-8) — 1400 отрезков (Гвадалквивир, Дуэро и т.п. — без этого
  порога у Испании на карте не было вообще ни одной реки).
- Итого 902 отдельные именованные реки в 1976 отрезках.

### Озёра — данные на будущее (`assets/lakes.json`, ничего визуально НЕ меняет)
[build_lakes.py](scripts/tools/build_lakes.py) — деление на большие
(≥500 км², `LARGE_LAKE_AREA_KM2`) и малые, под будущие механики (речные
суда/паромы через большие озёра, рыболовный промысел):
- большие (357) — Великие озёра Сев. Америки, Байкал, Виктория, Танганьика,
  Малави/Ньяса, Ладога, Онега, Балхаш, Титикака, Иссык-Куль, Венерн,
  Женевское, Чад, Чудское и т.п.;
- малые (936) — остальное (≥15 км², но <500 км²), без особых механик.
- Острова внутри озёр НАМЕРЕННО убираются (не переносятся как дырки) —
  по просьбе пользователя, озеро всегда сплошная вода.

### Найденные и исправленные баги
- **`IrregularCellProvider.gd` не поддерживал дырки в полигонах** — рисовал
  только внешний контур клетки, дырки (озёра) игнорировал полностью, поэтому
  Каспий/Байкал/Великие озёра/Виктория закрашивались как суша. Добавлена
  поддержка множественных колец (`rings[0]` внешний контур, `rings[1..]`
  дырки) — заливка чётно-нечётным правилом в `_fill_polygon`, границы рисуются
  по всем кольцам. Обновлены `build_land_sea.py`/`build_continents.py`, чтобы
  писать дырки (`p.interiors`) в JSON, а не только внешний контур.
- **Острова внутри озёр вылезали оторванными чёрными кляксами** — по ДВУМ
  независимым причинам, обе исправлены в `build_land_sea.py`/`build_continents.py`:
  1. Вычитание озера из суши делалось полигоном озера С его дырками-островами
     — острова физически не входили в вычитаемую область и оставались
     отдельными кусками суши посреди озера. Фикс: вычитать СПЛОШНОЙ контур
     озера (без дырок).
  2. Некоторые озёра (рифтовые озёра Африки, как Каспий) — не отдельная
     запись в `ne_10m_lakes`, а дырка, уже встроенная прямо в
     `ne_10m_land` — острова внутри такой дырки вообще не участвуют в
     вычитании озера. Общий фикс: любой кусок суши, ЦЕЛИКОМ лежащий внутри
     дырки ДРУГОГО куска (через `STRtree`/`contains` по центроиду) —
     остров в озере, выбрасывается целиком, независимо от источника.
- **Самопересекающиеся "бабочки"-полигоны после округления координат** —
  добавлена защита (`_emit_cell` в обоих скриптах: строим `Polygon(ext,
  holes)` из УЖЕ округлённых координат, если невалиден — `buffer(0)` и
  разбор на несколько простых клеток), та же схема, что и в
  `build_land_cells.py`/`_add_wavy_cell`.
- **Клавиши-символы слоёв (`=`, `-` и т.п.) не всегда читались из-за
  раскладки клавиатуры** — `event.keycode` зависит от раскладки, переключили
  `TileMapViewer.gd` на `event.physical_keycode` (та же категория бага, что
  раньше был с WASD).
- **Устаревший кэш классов Godot** — после добавления новых файлов со
  `class_name` (`RiverTileProvider.gd`, `BakedTileProvider.gd`) движок не
  сразу их подхватывал ("Identifier ... not declared", вся игра зависала на
  "Загрузка карты...") — чинится пересканированием проекта
  (`godot --headless --editor --quit-after N`).

### Договорились на будущее (см. TODO.md)
"Суша/Море" — единственный источник истины по форме берега; "Континенты" и
последующие слои (регионы, субрегионы и т.д.) должны не пересчитывать
береговую линию заново из сырых данных, а брать готовые куски прямо из
`land_sea.json` и просто размечать их (континент/регион и т.п. через
point-in-polygon по центроиду куска, используя внешние датасеты только как
справочник классификации, не как источник формы). Реализация этого перехода
для `build_continents.py` — ещё не начата, обсуждали в конце сессии.

## Мерцание при зуме и шов на слое "Континенты" (эта сессия)

### Мерцание/потемнение полупрозрачных слоёв при смене LOD
Раньше при смене уровня детализации старый тайл слоя держался как подложка,
пока НЕ ДОГРУЗИТСЯ ВЕСЬ видимый слой на экране целиком (проверка `missing[li]
> 0` по всему слою). Из-за этого старый и новый тайл полупрозрачного слоя
(континенты, альфа ~0.55) несколько кадров лежали друг на друге — альфа
складывалась (0.55 поверх 0.55 ≈ 0.8), слой заметно темнел на долю секунды
при каждом зуме.
- **Фикс**: в [TileMapViewer.gd](scripts/TileMapViewer.gd) добавлена
  `_replacement_ready(li, kz, kx, ky, lod, x0, y0, x1, y1)` — точная проверка
  "готова ли ЗАМЕНА именно ЭТОГО старого тайла" (для более мелкого LOD —
  все дочерние тайлы новой сетки в этой области; для более крупного —
  один родительский тайл нового LOD). Старый тайл снимается сразу, как
  только его конкретная замена готова, а не когда весь экран целиком.
  Убрана старая грубая `missing`-логика.
- Непрозрачные слои (спутник) это не задевало — там наложение старого/нового
  тайла незаметно, проблема была специфична для полупрозрачных оверлеев.

### Шов/полоса на слое "Континенты", "прыгающая" по широте при зуме
Пользователь заметил тонкую полосу через континент (видна на нескольких
широтах одновременно — Сибирь, Африка, и т.д.), которая обрывалась ровно на
границе континента (в океан не заходила) и перескакивала на другую широту
при смене уровня зума (напр. с Африки — на Сардинию).
- **Причина**: [bake_continents_tiles.py](scripts/tools/bake_continents_tiles.py)
  рендерит каждый тайл независимо в суперсэмплинге (x2), потом сжимает
  LANCZOS до 256×256 — но у фильтра НЕТ доступа к реальным пикселям ЗА
  краем тайла (тайл рендерится изолированно), поэтому у самого верхнего и
  нижнего ряда каждого тайла получается едва другой оттенок, чем у
  соседнего тайла того же ряда. На сплошной прозрачности (океан) незаметно,
  а рядом с любым внутренним краем (изгиб берега, озеро) — видна тонкая
  полоса через весь ряд тайлов. Разная тайловая сетка на разных `z` →
  разная широта шва на разных уровнях зума → "прыгает" при смене зума.
- **Фикс**: тайл теперь рендерится с запасом `MARGIN_PX=4` (в итоговых px)
  за каждым краем, сжимается ВМЕСТЕ с запасом (то же соотношение
  суперсэмплинга), затем обрезается ровно до нужных 256×256 — LANCZOS видит
  настоящих соседей, шов пропадает.
- Заодно, по просьбе пользователя, границы континентов сделаны тоньше/чётче:
  `BORDER_WIDTH: 0.7 → 0.45`, `MAX_BORDER_PX: 3 → 2`.
- **Важно**: правки внесены в `bake_continents_tiles.py`, но тайлы **ещё не
  перезапечены** — перезапечка отложена, чтобы сделать её один раз вместе
  со сглаживанием контуров континентов (см. TODO.md), а не дважды.

## Новый слой клеток суши v2 — генерация ГОТОВА, в игру НЕ подключён (эта сессия)
По плану TODO.md "слой клеток суши — ПЕРЕДЕЛАТЬ С НУЛЯ", но **клавиша 9,
не 8** (решение пользователя в этой сессии; 8 остаётся свободной).
Новый скрипт [build_land_cells_v2.py](scripts/tools/build_land_cells_v2.py) →
`assets/land_cells_v2.json`. Старый слой (клавиша 7) не тронут.
- **Метод принципиально другой, чем у старого** (Voronoi + shapely
  `intersection`, где были подтверждённые дыры с ненайденным механизмом):
  - регулярная сетка Voronoi-семян с джиттером 30% шага (клетки примерно
    равны по площади — явная просьба пользователя для первой версии);
  - **растровое разбиение вместо геометрического клипа**: полигон суши
    растеризуется в маску (PIL), каждый пиксель суши приписывается
    ближайшему семени (scipy KDTree), контур клетки восстанавливается
    `cv2.findContours` — покрытие без дыр по построению;
  - **новая зависимость**: `opencv-python-headless` (установлена).
- **Фаза 1 — только крупная суша** (> 50 000 км², константа
  `NEARSHORE_MAINLAND_KM2` переиспользована из старого скрипта): 32 массива,
  **14 109 клеток**, целевая площадь та же (`TARGET_CELL_AREA_KM2=10000`).
- **Verify-проверка встроена в скрипт** (сэмплирование точек внутри суши,
  как инструментальная проверка дыр из TODO.md): итог **98.83% покрытия**.
- Найденные и исправленные по пути баги:
  - per-cell `simplify()` создавал микрощели между соседними клетками
    (контуры одной физической границы упрощались независимо у двух соседей)
    — убран, покрытие было 91.6%;
  - независимая трассировка контуров (`findContours` на маске каждой клетки
    отдельно) давала тонкие щели 0.05–2 world-px на стыках — исправлено
    дилатацией маски клетки (`DILATE_PX=3`) с обрезкой по общей маске суши:
    соседние клетки слегка перекрываются вместо зазора.
- **Куски суши через антимеридиан по-прежнему пропускаются** (как в старом
  скрипте) — известное ограничение, см. пункт про "круглый мир" в TODO.md.
- **Осталось**: подключить в игру (провайдер + клавиша 9 в
  `TileMapViewer.gd._unhandled_input` + подсказки в `Main.tscn`), решить
  оставшиеся ~1.2% непокрытия, потом мелкие острова отдельным проходом.
- На будущее записана желаемая карта плотности клеток (пользователь, эта
  сессия): Европа и Япония/Корея — очень мелкие клетки, ЮВА и Сев. Америка —
  чуть крупнее, Африка/Юж. Америка — средние, Австралия — большие,
  Сибирь — очень большие, Сахара — самые большие.

## Прочее за эту сессию
- **`PRELOAD_MAX_Z = 5`** в [TileMapViewer.gd](scripts/TileMapViewer.gd):
  фоновая прогрузка всего мира (TilePreloader) урезана до z5
  (1 365 тайлов/источник вместо 21 845), интерактивный зум остался до
  `MAX_Z=7` — тайлы z6/z7 подгружаются по требованию для видимой области.
- **`texture_filter = NEAREST`** на спрайтах тайлов (`_make_tile`) — убирает
  подмешивание цвета соседнего тайла на стыке при билинейной фильтрации.
  (Мерцание при зуме это НЕ вылечило — его настоящая причина/фикс описаны
  выше, `_replacement_ready`.)
- **Слой "Континенты" запечён до z7 включительно** (сначала z6, потом
  пользователь решил включить и z7): 8 108 PNG, ~36 МБ с суперсэмплингом x2.
  Пользователь хотел x4 — договорились сначала посмотреть на x2 (вес почти
  не отличается, отличается время запекания; x4 не делали).
- **Диагностирована (не чинилась) медленная догрузка при скачке камеры**
  (Европа → Юж. Америка): нет приоритизации тайлов по близости к центру
  экрана (FIFO-очередь сети в `OnlineTileProvider` + raster-порядок обхода в
  `TileMapViewer`), TilePreloader конкурирует за сеть (свой пул из 3),
  `REQUEST_TIMEOUT=10 c` держит слоты. План фиксов проговорён, не внедрён.
- **TODO.md: добавлен пункт "Бесшовный 'круглый' мир"** (кругосветные
  путешествия: wrap камеры по модулю WORLD_PX, дорисовка тайлов у шва,
  дыра генераторов клеток на антимеридиане).
- Обсуждено (без кода): порядок слоёв иерархии — суша/море первым, клетки и
  верхние уровни (область→субрегион→регион→субконтинент→континент) можно
  параллельно, принадлежность клетки уровню — по центроиду; какие слои можно
  запекать в PNG (все статичные), какие нельзя (политический/экономический/
  религиозный и всё, что меняется в партии — им нужна точечная инвалидация
  кэша текстур); "острова на Великих озёрах" — настоящие острова из данных
  (Айл-Ройал, Georgian Bay), не артефакт.

## Континенты: сглаживание контуров + допечёка тайлов (эта сессия)
- [build_continents.py](scripts/tools/build_continents.py): добавлено
  сглаживание Чайкина (`chaikin_smooth`, 2 итерации) контуров континентов
  после `simplify()` — острые углы упрощённого полигона стали плавными.
  `continents.json` пересчитан.
- [bake_continents_tiles.py](scripts/tools/bake_continents_tiles.py):
  `SUPERSAMPLE` поднят 2→4 (по просьбе пользователя, после сравнения
  вариантов x2/x4/z10 — выбран x4 без углубления зума).
- **Найден и исправлен артефакт**: на низком зуме (z1–z3) появлялась ложная
  горизонтальная линия через весь тайл (обнаружено на стыке Дании/Швеции и
  на Босфоре) — причина: у гигантских слитых полигонов (Европа/Азия после
  Чайкина — 17–23 тыс. точек) на низком зуме не хватало пикселей на
  мировую единицу, тонкая деталь контура схлопывалась в суб-пиксельную
  полоску и PIL scanline-заливка протягивала сплошную линию через весь
  тайл. Фикс: `MIN_SCALE` — держим минимум px/мировая-единица на всех
  уровнях, поднимая supersample только для низких z (там тайлов мало,
  z0–z3 = 85 шт., дёшево).
- Тайлы перезапечены дважды (первый раз без фикса линии, обнаружен и
  исправлен, перезапечено снова) — **важный урок**: перед повторным запуском
  долгой печи ВСЕГДА проверять, что предыдущий процесс дейстительно
  остановлен (`ps aux`), а не полагаться на то, что `TaskStop`/новый запуск
  сам разберётся — был случай, когда старый и новый процесс печи работали
  ОДНОВРЕМЕННО в одну и ту же папку, портя результат.
- Итог: 8104 тайлов, 35.57 МБ.

## Суша/Море: PNG-запекание (эта сессия)
Тот же повод, что у континентов — `land_sea.json` ещё тяжелее (Евразия+
Африка слиты в ОДИН контур на 27830 точек, т.к. Суэц не гидрографический
разрыв, а перешеек/канал). Написан
[bake_land_sea_tiles.py](scripts/tools/bake_land_sea_tiles.py) по образцу
continents-версии (тот же MARGIN_PX-стык, тот же MIN_SCALE-фикс для
низкого зума). `TileMapViewer.gd` переключён на `BakedTileProvider`
(fallback на живой `IrregularCellProvider`, если PNG нет). Итог: 9978
тайлов, 41.21 МБ, ~81 минута печи.

## Клетки суши v2 — УДАЛЕНЫ (эта сессия, решение пользователя)
Сначала пробовали докрутить v2 (растровые Voronoi-клетки, см. раздел выше):
- добавлена региональная плотность по урбанизации (SPARSE_ZONES_V2/
  BOOST_ZONES_V2 — Европа/Япония/Корея гуще, Сибирь/Сахара крупнее);
- найден и исправлен серьёзный баг производительности: `_raster_voronoi_cells`
  сравнивала ПОЛНЫЙ растр на КАЖДУЮ клетку (`label_img == seed_idx`) —
  при равномерной плотности терпимо (~5400 клеток), при региональной
  (десятки тысяч клеток на растре, выросшем на порядок под густые зоны)
  практически бесконечно; фикс — `scipy.ndimage.find_objects` (один проход
  вместо N);
- исправлены двойные линии на границах клеток (раздутая для заливки
  геометрия использовалась и для границы — развели на два поля, `rings`
  для заливки и `brd` для границы, посчитанной из НЕраздутой маски);
- добавлено детерминированное (хэш-сид от координат ребра, НЕ `random`)
  волнение границ — превращает математически прямые Voronoi-рёбра в
  ломаные, с гарантией совпадения у двух соседних клеток.

**Итоговое решение**: полностью заменить синтетические Voronoi-клетки
РЕАЛЬНЫМИ административными границами (см. следующий раздел) — `build_land_cells_v2.py`
и `assets/land_cells_v2.json` удалены, слой и клавиша `8` убраны из
`TileMapViewer.gd`.

## НОВЫЙ слой "Области" — реальные admin1-границы (эта сессия)
Источник: **Natural Earth `ne_10m_admin_1_states_provinces`** (10m Cultural
Vectors, "Admin 1 – States, Provinces"), общественное достояние —
`https://naturalearth.s3.amazonaws.com/10m_cultural/ne_10m_admin_1_states_provinces.zip`.
8535 реальных административных единиц (штаты/области/республики/провинции)
по всему миру — включая Мордовию, Марий Эл, Галисию, любой штат США.
Новый скрипт [build_admin1_areas.py](scripts/tools/build_admin1_areas.py) →
`assets/admin1_areas.json`, слой "Области (реальные, admin1)" подключён в
`TileMapViewer.gd`, клавиша `8`.

Датасет оказался неоднородным по детализации — для некоторых стран/регионов
намешаны единицы совсем другого (более мелкого) уровня. Решалось поэтапно:

- **Точечное слияние Лондона**: `MERGE_GROUPS` — 29 London Borough слиты в
  один "Большой Лондон" (та же аномалия — Англия в среднем на уровне
  графств/unitary authorities, а Лондон почему-то на уровне районов).
- **Общий алгоритм слияния** (без хардкода стран, три независимых сигнала
  "мелкости", НЕ единый — метод перебирался несколько раз по ходу сессии):
  1. `type_en` из датасета (Municipality/Commune/Borough/Parish/Canton и
     т.п.) — оказалось НЕНАДЁЖНО само по себе: одна и та же метка означает
     разный масштаб в разных странах (в Македонии мелкие муниципалитеты
     почему-то помечены "Statistical Region" — тем же ярлыком, которым в
     Словении помечены КРУПНЫЕ области);
  2. **Локальное** сравнение площади с соседями (в LOCAL_RATIO=5 раз
     мельче среднего своих соседей той же страны) — устойчивее странового
     среднего (первая версия сравнивала с МЕДИАНОЙ по стране — оказалось
     сломано: в Словении 181 из 193 кусков УЖЕ мелкие муниципалитеты,
     поэтому сама медиана "тонет" в мелких значениях и почти не
     срабатывает);
  3. **Абсолютный порог** `ABSOLUTE_SMALL_KM2=1500` — самый агрессивный,
     добавлен по прямой просьбе пользователя ("увеличить агрессивность
     слияния"), сознательно принят риск задеть настоящие маленькие
     единицы (Македония/Руанда и т.п.), а не только явные аномалии.
  Мелкий кусок сливается с соседом (СТРОГО в той же стране, см. `admin`) по
  самой длинной общей границе; сосед поглощает и сохраняет своё имя/тип —
  цепочка кластеров мелких соседей схлопывается сама постепенно.
  Итог: 8503 → 5892 кусков.
- **Удаление мелких островов** (не слияние — полное исключение из вывода,
  через море "клякса"-геометрия выглядела бы хуже): порог 300 км²
  глобально, 100 км² для Карибского бассейна/Мальдив (`SOFT_ISLAND_ZONES`).
  Исключения (НЕ удаляются независимо от площади):
  - `PROTECTED_ISLAND_NAMES` — исторически значимые острова по имени (Иона,
    Скеллиг-Майкл, Мальта-локальности не подходят под это — см. ниже,
    Гори, Роббен-Айленд, Иводзима, Пасхи и т.д.);
  - `PROTECTED_ISLAND_ZONES` — защита по КООРДИНАТАМ, не по имени: нужна
    для случаев, когда у острова в датасете вообще НЕТ своего имени (он
    просто часть MultiPolygon у более крупной провинции) — Ваддензе/
    Фризские о-ва (Тексел, Терсхеллинг и т.п.) идут как безымянные куски
    провинции "Friesland"/"Groningen"/"Niedersachsen";
  - `PROTECTED_ISLAND_COUNTRIES` — защита по стране целиком: у Мальты датасет
    делит территорию на муниципалитеты (Валлетта, Слима и т.п.), даже слитые
    ПОЛНОСТЬЮ в естественные куски (остров Мальта/Гозо) всё равно мельче
    300 км² (вся Мальта ~246 км²) — обычная защита по имени острова тут не
    подходит вообще (имена — случайные "выжившие" после цепочки слияний,
    напр. "Xewkija"/"Birgu", а не "Malta");
  - `TOP_N_ISLANDS` + `color_key` — для Мальдив (все атоллы мельче даже
    мягкого порога 100 км², физически не касаются друг друга — обычное
    слияние их не объединит) оставлены top-5 крупнейших атоллов как
    ОТДЕЛЬНЫЕ (не слитые геометрически) клетки, но покрашенные ОДНИМ
    цветом — небольшое расширение формата (`"color_key"` в JSON,
    `IrregularCellProvider.gd` хэширует цвет по этой строке вместо
    порядкового индекса клетки, если поле есть).
  Итог: 5892 → 3436 кусков (2456 мелких островов удалено), финально
  **4039 клеток** в `admin1_areas.json`.
- **Известное ограничение**: `area_km2` каждой клетки НЕ записывается в
  итоговый JSON (только `rings`/`bbox`/`name`/опционально `color_key`) —
  считается внутри скрипта для слияния/фильтрации, но наружу не идёт.
  Пользователь сказал "потом опишу подробно" — не сделано, ждёт следующей
  сессии.

## Фундамент слоя данных "Клетка" + первый пример нарезки области на клетки (эта сессия)

### Cell.gd — данные клетки, без Region/City/реестра (по решению пользователя)
Прочитаны и обсуждены [ПЛОЩАДЬ_КЛЕТОК_И_ПОЛЕЗНОСТЬ.md](ПЛОЩАДЬ_КЛЕТОК_И_ПОЛЕЗНОСТЬ.md)
и [УРОВНЕЙ_ТЕРРИТОРИЙ.md](УРОВНЕЙ_ТЕРРИТОРИЙ.md). По прямому решению
пользователя ("только cell клетки, больше ничего") на этом шаге заведена
ТОЛЬКО сама клетка, без `Region`/`City`/реестра соседей — они заведомо
следующий шаг, не выдуманы заранее:
- [scripts/data/Cell.gd](scripts/data/Cell.gd) — `Resource` без геометрии
  (геометрия — забота слоя рендера, не данных): `id`, `area_km2`/`area_factor`,
  природные поля (relief/cover/soil/climate/moisture/features/resource),
  `settlement_factor`/`usable_land_factor`, освоение, население,
  `compute_rural_capacity()` (принимает плотность региона ЧИСЛОМ, не
  объектом `Region` — того ещё нет), `to_display_dict()` под будущую карточку
  UI (раздел 4 УРОВНЕЙ_ТЕРРИТОРИЙ.md).
- [scripts/data/GameplayTables.gd](scripts/data/GameplayTables.gd) —
  таблицы/формулы из диздока: `area_factor()`, `default_settlement_factor()`,
  `development_factor()`, `movement_cost()`, `control_cost()`.
- [scripts/data/CellCatalog.gd](scripts/data/CellCatalog.gd) — грузит
  `Cell`-объекты из готового JSON (сейчас `assets/cells_test.json`), id
  совпадает с id той же клетки в геометрии слоя рендера.
- Первая версия завела было и `Region.gd` — удалена по решению пользователя
  в этой же сессии (только Cell на этом шаге).

### Пример нарезки: провинция Ла-Корунья -> 4 клетки (новый слой, клавиша `C`)
По прямой просьбе пользователя ("разбей Ла-Корунью на 3-5 клеток, отрисуй")
сделан первый живой пример клеток поверх уже существующей геометрии
"Областей" (`admin1_areas.json`, клавиша `8`), без похода в сырые
Natural Earth данные:
- [scripts/tools/build_cells_test.py](scripts/tools/build_cells_test.py) —
  берёт готовый полигон "La Coruña" из `admin1_areas.json`, режет на 4
  клетки обычным Voronoi (`shapely.ops.voronoi_diagram`), считает
  `area_km2` каждой (обратная проекция Web Mercator + локальная
  равнопромежуточная проекция) -> `assets/cells_test.json`. Явно ТЕСТОВЫЙ
  скрипт (суффикс `_test`, см. новое правило именования в CLAUDE.md).
- Рендер — тот же `IrregularCellProvider.gd`, что и у остальных неровных
  слоёв, второй instance с другими параметрами стиля, новый слой "Клетки
  (тест: Ла-Корунья)" в [TileMapViewer.gd](scripts/TileMapViewer.gd),
  клавиша `C`.
- `CellCatalog.load_cells()` грузит те же 4 клетки как данные и печатает в
  консоль id/площадь/`area_factor` при старте — проверка, что геометрия и
  данные сходятся по количеству и площади (сумма ≈ реальная площадь
  провинции ~7950 км²).

### Найден и исправлен реальный баг: бесконечный цикл в фоновом потоке
При добавлении пунктирных границ (`_dash_segments_world` в
[IrregularCellProvider.gd](scripts/IrregularCellProvider.gd)) пользователь
столкнулся с зависанием игры и даже уходом компьютера в сон под нагрузкой.
Причина — классический баг off-by-epsilon: на границе штрих/промежуток
`remaining` из-за погрешности плавающей точки могло округлиться до ~0,
`seg_end == pos`, цикл `while pos < seg_len: ... pos = seg_end` не
продвигался — 100% одного ядра НАВСЕГДА в фоновом `WorkerThreadPool`-потоке,
причём на КАЖДЫЙ рендерящийся тайл слоя. Исправлено: `EPS`-защита
гарантированного шага + отдельный жёсткий потолок `MAX_DASH_SEGMENTS` про
запас. Написан регресс-тест
[scripts/tools/test_dash_no_hang.gd](scripts/tools/test_dash_no_hang.gd)
(автономный `SceneTree`-скрипт, `godot --headless --script ...`) — гоняется
после каждой правки этой области кода, показывает "OK: N штрихов посчитано
за M мс" вместо зависания.

### Стиль границ — толщина, чёткость, волнистость (эта сессия, несколько итераций по скриншотам)
Пользователь несколько раз присылал скриншоты и уточнял: итоговое решение —
`IrregularCellProvider.gd` получил набор НОВЫХ опциональных параметров стиля
(старые слои не задеты, дефолты = прежнее поведение):
- **Пунктир** — `border_dashed`/`dash_len`/`dash_gap`, фаза считается по
  накопленной длине в МИРОВЫХ координатах (не по локальным px тайла) — иначе
  на стыке двух тайлов штрихи не совпадают.
- **Мягкость сглаживания края** — `border_feather` (было жёстко 1.0 px).
- **Минимальная полутолщина** — `border_min_half_w` (было жёстко 1.0 px) —
  нашёлся баг: этот "пол" перебивал `border_width` на любом зуме ниже
  максимального, из-за чего изменение толщины визуально не давало эффекта
  ("граница не изменилась" — жалоба пользователя, разобрана и исправлена).
- **Разрешение растра тайла** — `raster_px` (было жёстко 256 для всех слоёв)
  — реальная причина "видны пиксели" на сильном приближении: тайл рисуется в
  фиксированное разрешение независимо от зума, `TileMapViewer._make_tile`
  сам подгоняет масштаб спрайта под `tile_world / texture.get_width()`, так
  что подъём `raster_px` работает "бесплатно" для рендера, но НЕ бесплатно
  по CPU/памяти — обсуждали с пользователем: для крошечного тестового слоя
  клеток (1-2 тайла) это несущественно, для слоя "Области" (весь мир) это
  реально 16× работы на КАЖДЫЙ тайл; пользователь осознанно попросил
  `raster_px=1024` для обоих слоёв, зная цену (см. TODO.md).
- **Волнистые внутренние рёбра** — `build_cells_test.py`: тот же приём
  рекурсивного смещения середины отрезка, что уже применялся для береговой
  линии (`build_land_cells.py`), но детерминированный по хэшу КЛЮЧА РЕБРА
  (отсортированные округлённые концы, не зависит от направления обхода) —
  волна применяется к СЫРЫМ Voronoi-регионам ДО обрезки провинцией, поэтому
  внешний контур области остаётся прямым (как в исходных данных), волнистые
  только внутренние рёбра между клетками.
- **Именованный стиль вместо позиционных чисел** — по прямому запросу
  пользователя (принёс пример формата `const BORDER_STYLE := {...}`),
  признано ЛУЧШЕ прежнего подхода (позиционные аргументы с комментариями) —
  заведён `const BORDER_STYLE` в `TileMapViewer.gd` с записями `"cell"` и
  `"province"` (только для того, что реально отрисовано — `"region"`/
  `"country"` не заводить заранее без геометрии).
- Итоговые числа (подобраны под ~1.5px/alpha 0.30 для клеток и ~2.5px/alpha
  0.55 для областей НА БЛИЗКОМ ЗУМЕ z7, см. формулу в комментарии
  `BORDER_STYLE`): `cell.width=0.375`, `province.width=0.625`.

### Убрано "мыло" на стыке границы клетки и границы области
Пользователь заметил по скриншоту: там, где контур клетки совпадает с
границей самой провинции, линия выглядит размытой — причина: ДВЕ почти
совпадающие полупрозрачные сглаженные линии рисуются друг на друге (своя у
слоя клеток + своя у слоя области), с независимым сглаживанием и чуть
разными округлениями координат. Исправлено на уровне данных, не рендера:
- `build_cells_test.py`: `_split_open_border_chains()` — для каждого ребра
  клетки считает расстояние от его середины до ИСХОДНОГО (ещё не порезанного
  на клетки) контура провинции; рёбра ближе `BOUNDARY_TOL=0.05` мировых px
  классифицируются как "внешние" (совпадают с границей области, её и так
  рисует слой "8") и выбрасываются из отрисовки; остальные — "внутренние"
  (волнистые Voronoi-рёбра) — идут в новое поле JSON `brd_open` списком
  ОТКРЫТЫХ (не замкнутых) цепочек точек.
- `IrregularCellProvider.gd`: новое поле `border_open_rings` (парсится из
  `brd_open`) — когда непусто, ПОЛНОСТЬЮ заменяет `border_rings`/`rings` как
  источник линии для конкретной клетки, рёбра рисуются БЕЗ замыкания
  последней точки на первую (в отличие от `border_rings`/`rings`, которые
  всегда кольца). `_dash_segments_world()` получил параметр `is_open` для
  того же (не заворачивать пунктир через несуществующее последнее ребро).
  Важно: `rings` (заливка/площадь/будущий клик) НЕ меняется — это чисто
  вопрос отрисовки линии, геометрия клетки как источник истины осталась
  прежней (объяснено пользователю, который прямо спросил "как клетка потом
  поймёт, где её границы").
- Метод общий, не привязан к Ла-Корунье конкретно — сработает для любой пары
  "контейнер -> нарезанные внутри него клетки" (подтверждено пользователю на
  вопрос "будет ли работать для области/страны").
- По пути найдена и исправлена ошибка компиляции GDScript (`Cannot infer the
  type of "is_open"/"edge_count"` — `Variant` из `Dictionary`-значения без
  явного каста не даёт статически вывести тип для `.is_empty()`/`.size()`),
  до этого regression-тест на зависание "проходил", но реально не собирался
  весь `TileMapViewer.gd` — урок: тестировать не только изолированный кусок
  кода, но и headless-компиляцию всего проекта после каждой правки.

### На будущее (записано по ходу сессии, не начато)
- **Слой "Области" рано или поздно тоже пойдёт на офлайн-запечку в PNG**
  (как уже сделано для континентов/суши-моря, `bake_continents_tiles.py`/
  `bake_land_sea_tiles.py`) — тогда живой `raster_px=1024` для этого слоя
  станет не нужен (это временная затрата до запечки, не постоянная
  архитектура). Пользователь поднял это сам, вслух согласились, но
  запекание слоя "Области" в этой сессии не начато.
- **Неограниченный рост кэша текстур тайлов** (уже был известный пункт в
  TODO.md) — на практике в этой сессии процесс игры разросся до ~5.8 ГБ
  памяти при активном тестировании слоя "Области" на `raster_px=1024`
  (4 МБ на тайл, весь мир, без вытеснения из кэша) — подтверждает, что этот
  TODO-пункт стоит закрыть раньше, чем предполагалось.
- **Переименование `admin1_areas.json`** — отдельно зафиксировано и в
  TODO.md, и в CLAUDE.md, отложено пользователем на "после клеток".
- Прочитан и обсуждён [optim_map_godot_strategy.md](optim_map_godot_strategy.md)
  (документ пользователя с рекомендациями по архитектуре карты) — согласован
  почти полностью, записан как правило в CLAUDE.md (см. файл, раздел
  "Архитектура карты").

## Ревизия проекта и чистка старых слоёв (2026-07-09)
Пользователь принёс два внешних документа (архитектура и стек глобальной
стратегии) — оба разобраны, стек-документ частично отклонён (см. CLAUDE.md,
раздел "Стек проекта": предложение "C# = ядро игры" отклонено, принят гибрид
по типу вычисления). По итогам аудита CLAUDE.md/README.md/TODO.md/done.md
на устаревшие места:
- **`admin1_areas.json` → `assets/provinces.json`**, `build_admin1_areas.py`
  → `scripts/tools/build_provinces.py` — наконец выполнено (было отложено
  с прошлой сессии). Все ссылки в коде (`TileMapViewer.gd`,
  `IrregularCellProvider.gd`, `build_cells_test.py`) поправлены.
- **Слой "Клетки" (клавиша 5, `CellTileProvider.gd`) и слой "Клетки суши
  (неровные)" (клавиша 7, `land_cells.json`/`build_land_cells.py`) — удалены
  полностью** (код, JSON-данные, генератор), по явному решению пользователя:
  оба заменены реальной геометрией (`provinces.json` + тестовый слой игровых
  клеток `cells_test.json`, слой "Клетки (тест: Ла-Корунья)" на клавише `C`).
  Ранее (см. запись выше, "Про старый слой клеток суши") планировалось
  держать старый слой 7 "как образец для сравнения" — это решение отменено.
  Клавиши `5` и `7` сейчас свободны.
  - Заодно удалён `_diag_coverage.py` — диагностический скрипт, уже
    нерабочий (ссылался на `land_cells_v2.json`, удалённый ещё раньше) и
    зависевший от `build_land_cells.py`.
  - `preloader.setup([satellite, _cells], ...)` → `preloader.setup([satellite], ...)`
    — предзагрузка данных высот (Terrarium elevation tiles) была нужна
    только удалённому `CellTileProvider.gd`. Диски `user://elevcache`/
    `assets/tiles_bundle/elevcache` (~996 МБ) теперь осиротели — не удалены
    автоматически, см. TODO.md.
- README.md переписан под актуальное состояние (было: v0.2, слои 1-7;
  стало: актуальный список 10 слоёв/клавиш, актуальная структура `scripts/`).
- В CLAUDE.md добавлен раздел-индекс всех markdown-документов проекта
  ("что где искать"), чтобы не терять контекст между документами так же,
  как раньше терялся между файлами данных.
- `PLAN_RAZRABOTKI_GLOBAL_STRATEGY.md` помечен как видение на годы вперёд,
  не рабочий план (рабочий план — TODO.md).

## Бюджет создания текстур за кадр (2026-07-09)
Пользователь принёс отдельный внешний отчёт по оптимизации кода карты —
разобран, большинство пунктов совпало с уже сделанным/уже решённым (не
переходить на C#, не трогать debug/bake-слои раньше времени), часть пунктов
(admin1/CellTileProvider) была уже неактуальна на момент отчёта (см. ревизию
выше). Из реальных пунктов сделан один — остальные (лимит/LRU кэша тайлов,
запекание рек/морей, разделение request/sprite/texture-лимитов, строковые
ключи, `Cell.gd` → packed-массивы для будущей симуляции) сознательно
отложены: лимит кэша — по прежнему решению пользователя (не трогать, пока
мир не докачан), запекание — "потом", остальное — не горит.

**`MAX_TEXTURE_CREATES_PER_FRAME := 16`** добавлен в четыре провайдера,
которые пачкой создают `ImageTexture.create_from_image()` из результатов
фоновых потоков: `OnlineTileProvider.gd`, `BakedTileProvider.gd`,
`IrregularCellProvider.gd`, `RiverTileProvider.gd`. Раньше `_process()`
забирал ВСЕ готовые `Image` из `_done_images` и создавал текстуры для всех
разом — если резкий разворот камеры/смена слоя "дозревал" сразу много
фоновых задач одним кадром, это могло дать микрофриз. Теперь обрабатывается
не больше 16 штук за кадр, остаток (`done.slice(i)`) кладётся обратно в
`_done_images` (с тем же мьютексом, что и у фоновых потоков) и доделывается
в следующих кадрах — спрайт просто появляется на кадр-два позже, без
видимого рывка. `_decoding`/`_rendering` для необработанных элементов не
трогается, поэтому `request_tile()` для них корректно продолжает
возвращать `null` ("ещё не готово"), повторный запуск декодирования/рендера
не запускается.

## Клик по клетке → показатели из ПЛОЩАДЬ_КЛЕТОК_И_ПОЛЕЗНОСТЬ.md (2026-07-09)
Пользователь прочитал/принёс `ПЛОЩАДЬ_КЛЕТОК_И_ПОЛЕЗНОСТЬ.md` и попросил сразу
же посчитать показатели документа для тестовых клеток Ла-Коруньи и показывать
их по клику на карте. Реализовано и проверено вручную (запуск игры, скриншот
клика по клетке 4 — площадь/area_factor/settlement_factor/население совпали
с ожидаемыми формулами).

- **`GameplayTables.gd`** — добавлены временные тестовые константы
  "региона" (`TEST_GALICIA_BASE_RURAL_DENSITY`/`_INFRASTRUCTURE_FACTOR`/
  `_HEALTH_FACTOR`/`_SECURITY_FACTOR`), т.к. полноценного `Region.gd` в
  проекте ещё нет — взяты из примера раздела 7 документа (Галисия, 35
  чел/км², 1.10/0.95/1.00). Заменить настоящим `Region`, когда он появится.
- **`scripts/tools/build_cells_test.py`** — добавлен `TEST_CELL_ATTRS`:
  тестовые игровые атрибуты (рельеф/покров/почва/климат/освоение/ресурс/
  дороги) для всех 4 клеток Ла-Коруньи, ИЛЛЮСТРАТИВНЫЕ (не проверенная
  реальная география конкретно по клеткам, специально для разных значений в
  демонстрации формул) — явно помечено в коде. `cells_test.json`
  перегенерирован, площади клеток совпали с предыдущим прогоном (детерминизм
  seed=42 подтверждён).
- **`CellCatalog.gd`** — при загрузке теперь читает все новые поля из JSON,
  считает `settlement_factor` (дефолт по `relief_type` из GameplayTables,
  если не переопределён), `rural_capacity` (раздел 5 документа, через уже
  существующий `Cell.compute_rural_capacity()`) и `rural_population` (доля от
  ёмкости по `maturity`). Раньше грузились только `id`/`name`/`area_km2`.
- **`Cell.gd`** — `to_display_dict()` дополнен блоками `area` (area_km2/
  area_factor) и `factors` (settlement_factor/usable_land_factor) — это
  сознательное исключение из правила "без сырых множителей" специально для
  отладочной карточки по клику, проверяющей формулы документа.
- **`IrregularCellProvider.gd`** — новый публичный метод `get_cell_id_at(pos)`:
  простой перебор всех клеток провайдера (point-in-polygon по `Geometry2D.
  is_point_in_polygon`, с учётом дырок-колец) — нормально для маленьких
  bake/test-слоёв (единицы-десятки клеток), для крупных живых слоёв в
  будущем нужен ID-карта/spatial-index (см. CLAUDE.md/optim_map_godot_strategy.md).
- **`TileMapViewer.gd`** — ЛКМ по карте (когда слой "Клетки (тест: Ла-Корунья)"
  включён и `MarkTool` не активен) конвертирует клик в мировые координаты
  (`camera.get_global_mouse_position()`), находит клетку через
  `get_cell_id_at()`, ищет соответствующий `Cell` по id и показывает его
  `to_display_dict()` текстом в новой панели (`_cell_info_label`, верхний
  правый угол экрана). UI только форматирует уже готовый ViewModel, сам
  ничего не считает.
- **`Main.tscn`** — подсказки управления обновлены под текущий список слоёв
  (заодно убраны упоминания уже удалённых слоёв 5/7/admin1, см. более раннюю
  ревизию выше) и добавлена строка про клик по клетке `C`.
- Godot-проверка: headless `--check-only --script` по всем 9 изменённым
  файлам — синтаксис ОК. Полная headless-проверка `--check-only --path .`
  (без `--script`) зависает на импорте ~1 ГБ `tiles_bundle` — для точечной
  проверки синтаксиса использовать конкретный файл, не весь проект.
  Визуальная проверка — реальный запуск игры, скриншот клика по клетке 4
  ("Ла-Корунья — клетка 4", hills/pasture, 2428.2 км², area_factor=1.56,
  settlement_factor=0.75, население 16652/33304) — совпадает с ожиданиями.
- **Известный визуальный нюанс (не исправлено)**: панель карточки клетки
  впритык умещается по вертикали в правом верхнем углу — при более длинном
  тексте (напр. больше "особенностей") может обрезаться. Пользователь пока
  не просил поправить.

## Морские клетки: генератор + слой "Мировой океан" (2026-07-10)
Начата реализация АРХИТЕКТУРА_МОРСКИХ_КЛЕТОК.md. Работали ТОЛЬКО на
тестовом регионе (сейчас Франция+Испания+Бретань+Ла-Манш, см.
`TEST_REGION_LONLAT` в генераторе) — полный мир сознательно не печём, пока
всё не отточено (следующая сессия начнётся с глубин, см. TODO.md).

### `scripts/tools/generate_sea_cells.py` — генератор игровых морских клеток
Пайплайн по документу: водная маска → адаптивные seed-точки через
квадродерево (гуще у берега, реже в океане, целевой размер по кривой
расстояния до берега) → Voronoi → обрезка по морю → чистка/слияние мелких
кусков → граф соседства → [assets/generated/sea_cells.json](assets/generated/sea_cells.json)
+ `sea_cell_graph.json`. Рендерится debug-слоем `IrregularCellProvider` на
клавише `9` ("Клетки моря (тест)").

- **Источник суши — `assets/provinces.json`, НЕ `land_sea.json`** — явное
  решение пользователя: "слой провинций это основополагающий слой, на
  котором будет строиться вся карта дальнейшая" (см. память проекта
  `project_provinces_foundation_layer`). Все новые офлайн-генераторы суши
  по умолчанию берут `provinces.json`.
- **Найдены и исправлены ДВЕ квадратичные по стоимости "зависалки"**
  (первый прогон висел 10+ минут без результата на маленьком тестовом
  регионе, второй раз — 5+ минут на полном мире без единого прогресс-принта):
  1. `clip_and_explode` пересекал КАЖДУЮ Voronoi-клетку с ОДНИМ гигантским
     `sea_mask` (весь мир минус вся суша). Исправлено: STRtree по кускам
     суши, вычитаются только БЛИЗКИЕ куски (обычно 0-3), не вся геометрия.
  2. `compute_brd_open` считал `distance()` до ГЛОБАЛЬНОГО
     `land_union.boundary` на каждое ребро каждой клетки — тот же класс
     бага. Исправлено аналогично, через STRtree.
  3. **Реинкарнация бага при переходе на полный мир**: `load_land_union()`
     делал `unary_union()` всех провинций — соседние провинции Евразии
     склеивались в ОДИН полигон на 28216 точек с bbox почти во весь мир,
     и STRtree переставал быть локальным (для любой клетки рядом с
     Евразией индекс находил именно эту громадину). Исправлено —
     `load_land_pieces()` возвращает НЕсклеенные куски (по одной провинции),
     STRtree строится по ним; `unary_union` делается только ОТДЕЛЬНО, для
     растровой маски (где склейка не мешает).
- **Волнистые границы / скруглённые углы — попробованы и ОТКАЧЕНЫ**:
  сначала добавили волнение рёбер (тот же приём, что у
  `build_cells_test.py` — рекурсивное смещение середины ребра по хэшу),
  понравилось, но было слишком "мелко-пильчато" (depth 4→2). Дальше
  попробовали "круглые квадраты" (регулярная сетка seed-точек + `buffer(-r)
  .buffer(+r)` скругление углов) — пользователь забраковал ("ужасно"),
  попросил вернуть ПЕРВУЮ версию: хаотичные seed-точки (jitter=0.35), БЕЗ
  волнения, прямые линии Voronoi. Код волнения/скругления удалён (не
  оставлен мёртвым/закомментированным, см. CLAUDE.md).
- **Статус "потенциальный центр провинции"** — `Cell.gd` получил новое поле
  `province_center_status` ("" / "potential" / зарезервировано "actual").
  В `build_cells_test.py` помечена клетка `lacoruna_3` (розовая на карте,
  индекс 3 → hue≈307° по золотому сечению в `IrregularCellProvider._load_data`).
  В генераторе морских клеток — `merge_capital_coastal_cells()`: все морские
  клетки, касающиеся берега такой клетки, сливаются в ОДНУ (чтобы столичная
  клетка была со всех сторон окружена одной морской клеткой, а не
  разрезана внутренним sea-sea ребром). Пока связка ТЕСТОВАЯ (источник —
  `cells_test.json`, единственный файл с этим полем сейчас) — когда
  появится настоящий слой клеток суши, брать оттуда.
- **Кривая размера клетки от расстояния до берега** — несколько раз
  перенастроена пользователем в разговоре, финальная версия:
  `SIZE_DIST_KM=[0,100,300,500]` → `SIZE_TARGET_KM=[100,200,300,400]` км
  (0км→100км, 100км→200км, 300км→300км, 500км+→400км, дальше не растёт).
- Готовый формат клетки: `id/name/surface/rings/brd_open/bbox/center/
  area_km2/neighbor_sea_ids/coast` — `neighbor_land_ids` из документа НЕ
  заполняется (честно, не выдумано): в проекте пока нет слоя клеток суши со
  стабильными id, только `coast: bool`.

### `scripts/tools/build_world_ocean.py` + слой "Мировой океан" (клавиша `2`)
Отдельная, более простая задача (по прямой просьбе пользователя) — ЕДИНАЯ
маска "вода/не вода" на весь мир, БЕЗ деления на клетки, перед тем как
резать клетки на всём мире. Результат — [assets/world_ocean.json](assets/world_ocean.json)
(тот же формат rings/bbox, что у `land_sea.json`), вычислено как
`world_box - union(provinces.json)`.

- **Щели между провинциями → мнимое "море" внутри материков** — соседние
  провинции в `provinces.json` не всегда идеально стыкуются (обычное дело
  для реальных административных векторных данных). Без исправления —
  сотни мелких дырок-крапинок мнимой воды на стыках (найдено визуально на
  побережье Ла-Манша/Бретани/Португалии, сотни клеток < 5 км² с нулём
  соседей в `generate_sea_cells.py` тоже были тем же багом на побережье
  Франции/Испании).
- **Простой `buffer(+gap)` на каждый кусок ДО объединения — тоже
  попробован и заменён**: закрывал щели, но раздувал контур ~500
  фейковыми точками-скруглениями на каждый залив, которые `simplify` потом
  СХЛОПЫВАЛ, теряя реальную детализацию берега (111 точек на Ла-Корунью
  вместо 232 у provinces.json). Заменено на **морфологическое ЗАМЫКАНИЕ**
  (`buffer(+CLOSE_PX)` → `unary_union` → `buffer(-CLOSE_PX)`, CLOSE_PX=0.15) —
  щели заклеиваются, берег возвращается почти на исходное место, точек
  берега получилось БОЛЬШЕ, чем у простого buffer (1137 против 679 в
  тестовом окне). Плюс отдельная чистка результата: мелкие дырки-крапинки
  (`MIN_HOLE_AREA_PX2=4.0`) и лужи-щепки (`MIN_WATER_PIECE_AREA_PX2=4.0`)
  выброшены явно (208 дырок и 614 щепок в тестовом прогоне).
- **Реки не слиты с океаном в один полигон** — сознательное решение после
  обсуждения с пользователем: реки (`rivers.json`) — полилинии с уровнями
  значимости (`tier` 0/1/2 крупная/средняя/малая, уже готово в данных из
  `build_rivers.py`), это будущий ГРАФ движения (устье → узлы реки → порт,
  с ограничением по размеру корабля через `tier`), а не площадь — слияние
  в полигон уничтожило бы именно то, что нужно для этой механики. Вместо
  слияния — клавиша `2` включает слои "Мировой океан" И "Реки" ОДНОВРЕМЕННО
  (`_ocean_layer_idx` в `TileMapViewer.gd`, индекс слоя "Реки" захардкожен
  как `4` после переиндексации слоёв, см. ниже).
- **Клавиша `2`, не `O`** — изначально была `O` (буква, мнемоника
  "океан"), пользователь заметил визуальную путаницу О/0 с соседним слоем
  "Континенты" (клавиша `0`), переставлено на `2` (освободилась после
  удаления процедурных слоёв, см. ниже).

### Удалены процедурные заглушки: Политический/Экономический/Религиозный
По прямой просьбе пользователя ("надо удалить слои 2,3,4") — эти три слоя
(`OverlayTileProvider` поверх `PhysicalTileProvider`, случайный
шум/градиент, TODO.md годами просил заменить реальными данными провинций,
до реализации так и не дошло) удалены полностью, не оставлены как мёртвый
код:
- Убраны из `_layers` в `TileMapViewer.gd` вместе с `physical`-переменной.
- Файлы `scripts/PhysicalTileProvider.gd`/`scripts/OverlayTileProvider.gd`
  удалены (использовались только этими тремя слоями).
- **Все клавиши слоёв переиндексированы** (позиционная привязка `idx` в
  `_unhandled_input` завязана на порядок в `_layers`, сдвиг на 3 позиции):
  `1`=Спутник, `6`=Моря, `0`=Континенты, `-`=Суша/Море, `=`=Реки, `8`=Области,
  `C`=Клетки(тест), `9`=Клетки моря(тест), `2`=Мировой океан. Клавиши `3`,
  `4`, `5`, `7`, `O` теперь свободны.
- `README.md` поправлен (убраны строки про удалённые провайдеры).

### Пиксельность берега на слое "Мировой океан" — причина и решение (bake)
Пользователь заметил, что берег у слоя "Мировой океан" заметно "лесенкой"
по сравнению со слоем провинций при одинаковом `raster_px=1024`. Разобрано
по шагам, ложные версии по пути:
1. Проверили детализацию геометрии самого контура (124 точки у Ла-Коруньи
   в `world_ocean.json` против 232 у `provinces.json`) — НЕ причина, этого
   достаточно для гладкого контура.
2. **Настоящая причина**: `_fill_polygon` в `IrregularCellProvider.gd`
   рисует заливку БЕЗ сглаживания (скан-линия, `ceil`/`floor` до целого
   пикселя) — у провинций это незаметно, потому что поверх рисуется
   ОТДЕЛЬНАЯ обводка контура с настоящим феатером/градиентом
   (`border_feather`), которая визуально маскирует грубый край заливки.
   Обводка океана изначально была `alpha=0.0` (задумывалась "просто
   заливка без рамки") — поэтому был виден голый пиксельный край. Добавлена
   видимая (но мягкая, `border_feather=2.0`) обводка — помогло частично.
3. **Настоящая-настоящая причина мыла/зависания на близком зуме**: океан —
   ОДИН полигон на 34155 точек с bbox почти во весь мир, каждый тайл
   вынужден пересчитывать все точки (build-line заливка на чистом
   GDScript — то же самое, из-за чего "Континенты"/"Суша-Море" уже раньше
   переведены на запечённые тайлы, см. записи выше в этом файле). Пока
   тяжёлый живой рендер не досчитался — виден растянутый тайл с дальнего
   зума (мыло/пиксели).
- **Решение — запекание**: [scripts/tools/bake_world_ocean_tiles.py](scripts/tools/bake_world_ocean_tiles.py)
  (по образцу `bake_land_sea_tiles.py`/`bake_continents_tiles.py`,
  supersample 4x + LANCZOS даунскейл). ТЕСТОВЫЙ прогон печёт только регион
  (Северо-Запад Иберии, `REGION_LONLAT`), не весь мир — 66 тайлов за 78
  секунд. `TileMapViewer.gd` подключает `BakedTileProvider`, если папка
  `assets/tiles_bundle/world_ocean_baked` существует (тот же паттерн, что у
  континентов/суши-моря), иначе падает обратно на живой `IrregularCellProvider`.
  Пользователь подтвердил результат ("все норм! четко"). **Полный мир НЕ
  запечён** — сознательно отложено до конца доработки слоя (следующая
  сессия начнётся с механики глубин, см. TODO.md).

### Правило в CLAUDE.md: кто запускает/проверяет игру
Добавлен раздел "Проверка в игре — только пользователь, но запускать игру
обязательно" — Клод ОБЯЗАН сам (пере)запускать Godot после каждого
изменения, влияющего на видимое в игре, но саму визуальную ПРОВЕРКУ
(смотреть, скриншотить, кликать, оценивать) делает только пользователь.
Раньше Клод пытался сам разбираться через скриншоты — пользователь явно
попросил этого не делать.

## Батиметрия (превью), фикс береговой линии, запечённый тест провинций Галисии (2026-07-10, продолжение)

### Превью реальной батиметрии (клавиша `5`)
Первый шаг механики глубин (см. TODO.md прошлой записи) — референс-картинка
реальной батиметрии на тестовом регионе, ПЕРЕД тем как рисовать зоны глубины
вручную:
- [scripts/tools/_preview_sea_depth.py](scripts/tools/_preview_sea_depth.py) —
  качает грид GMRT (агрегирует GEBCO + доп. батиметрические съёмки) через их
  публичный GridServer (`https://www.gmrt.org/services/GridServer`, без
  ключей/регистрации), классифицирует на 4 зоны глубины (мелководье 0-30м /
  шельф 30-200м / склон 200-2000м / глубина 2000м+ — согласовано с
  пользователем), рендерит в той же проекции Web Mercator/WORLD_PX, что и
  весь остальной пайплайн. Суша — прозрачна (слой ложится ТОЛЬКО на воду).
  Регион обрезки — **тот же**, что у реального тестового "Мирового океана"
  (`REGION_LONLAT = (-10.5, 41.0, -6.0, 44.5)`, Галисия/СЗ Иберии) — важный
  урок сессии: первая версия брала регион генератора морских клеток (вся
  Западная Европа), из-за чего слой визуально "накрывал" куда больше, чем
  реально существующий тестовый водный слой.
- Ресайз категориальных зон — **NEAREST по индексу зоны**, палитра применяется
  ПОСЛЕ ресайза (не LANCZOS по готовым RGBA) — иначе прозрачная суша
  (RGB нулевой, alpha=0) подмешивает чёрный в цвет соседних пикселей на
  границе суша/море при сглаживающем ресайзе.
- Новый debug-слой [SeaDepthPreviewLayer.gd](scripts/SeaDepthPreviewLayer.gd) —
  одна статичная запечённая картинка (не тайлы, не живой рендер), позиционируется
  по мировым координатам из `assets/generated/sea_depth_preview_test_region_bbox.json`.
  Клавиша `5`. ТОЛЬКО референс для будущей ручной раскраски зон (MarkTool,
  следующая сессия) — не игровые данные.

### Найден и исправлен реальный баг: "Мировой океан" не совпадал с точным контуром провинции
Пользователь заметил на скриншоте нестыковку между слоем "Мировой океан"
(клавиша `2`) и слоем "Клетки (тест: Ла-Корунья)" (клавиша `C`) — на стыке
видна лишняя тёмная линия.
- **Причина** (найдена инструментально, не на глаз): `build_world_ocean.py`
  применял морфологическое замыкание (`buffer(+0.15)→union→buffer(-0.15)`,
  см. запись предыдущей сессии) КО ВСЕМ 4039 провинциям сразу — хотя реальная
  щель с соседом была только у 568 из них. У остальных (включая Ла-Корунью,
  которая и БЕЗ всякого буфера уже идеально стыкуется с соседями — проверено:
  она в одной группе с континентальной Европой при `CLOSE_PX=0`) замыкание
  всё равно применялось и портило настоящие детали берега (риасы у́же 0.3 px)
  чисто как побочный эффект, не имеющий отношения к щелям.
- **Фикс**: [build_world_ocean.py](scripts/tools/build_world_ocean.py)
  переписан — через `STRtree` находятся ТОЛЬКО куски с реальной щелью
  (раздутые тестовые копии пересекаются, а исходные контуры — нет), замыкание
  применяется ТОЧЕЧНО к ним; остальные (подавляющее большинство, ~87%) идут
  в `land_union` своим точным исходным контуром без единого буфера.
  `world_ocean.json` и тайлы `world_ocean_baked` (тестовый регион)
  перегенерированы. Фикс проверен инструментально — все 8 ранее найденных
  точек расхождения (см. предыдущую сессию) теперь согласованы.

### Новое правило в CLAUDE.md: язык — всегда русский
Пользователь несколько раз указывал, что Клод сбивается на английский —
оказалось, дело было в англоязычных описаниях Bash-вызовов (поле
`description`, тоже видно в интерфейсе), не только в основном тексте ответа.
Добавлен явный раздел в CLAUDE.md.

### Тестовый запечённый слой провинций — Галисия (клавиша `4`)
Пользователь попросил проверить более высокое разрешение (`SUPERSAMPLE=8`,
`TILE_PX=1024`) для запекания слоя "Области" (живой слой `8` не трогать).
Несколько итераций, каждая находила и чинила реальную проблему:

1. **Первая версия — весь Иберийский полуостров** (`bake_provinces_iberia_tiles.py`)
   — позже полностью удалена и заменена версией **только для Галисии**
   (`scripts/tools/bake_provinces_galicia_tiles.py`, тот же тестовый регион,
   что у "Мирового океана"/превью глубин) — по прямой просьбе пользователя,
   когда встал вопрос отдельного тестового слоя.
2. **Баг: тёмный "ободок"/кляксы на границе заливка↔прозрачность** —
   классический баг непремультиплицированной альфы при LANCZOS-ресайзе
   (прозрачный фон хранится как чёрный `(0,0,0,0)`, чёрный подмешивается в
   цвет соседних полупрозрачных пикселей). Фикс — `_resize_premultiplied()`
   (домножение RGB на альфу перед сжатием, обратное деление — ПОСЛЕ, на уже
   маленькой итоговой картинке, не на исходном супersample-холсте — иначе
   на дальнем зуме, где `MIN_SCALE` поднимает supersample очень высоко,
   float-массив на весь холст не влезает в память, ловили `numpy.
   _core._exceptions._ArrayMemoryError`).
3. **По просьбе пользователя границы сначала полностью убраны** (`alpha=0`,
   как у живого слоя `8` в этот момент) — оказалось, что "ложная граница",
   которую видел пользователь, была не отрисовкой, а щелями между
   провинциями (см. диагностику `world_ocean.json` выше) — исправлено
   раздутием (`GAP_FIX_PX=0.15`) каждой провинции индивидуально перед
   заливкой (только для картинки, не меняет `provinces.json`).
4. **Разрешение тайла поднято 256→1024** (`TILE_PX`, не просто `SUPERSAMPLE`)
   — по просьбе пользователя, когда края провинций выглядели "пиксельными"
   при близком зуме. Объяснено пользователю на пальцах: тайл — квадратный
   кусок ГОТОВОЙ картинки (как в Google Maps), не провинция; итоговый размер
   тайла у всех старых bake-скриптов жёстко зашит в 256×256, `SUPERSAMPLE`
   влияет только на чёткость ДО сжатия обратно в этот размер — увеличение
   именно `TILE_PX` даёт реально больше пикселей на тот же кусок мира.
5. **Граница возвращена** по прямой просьбе — тонкая (`width=0.15` мировых
   px), резкая (`feather=0.1`, минимум без деления на ноль), непрозрачная
   (`alpha=1.0`) — синхронно и в bake-скрипте, и в `BORDER_STYLE["province"]`
   живого слоя `8` в [TileMapViewer.gd](scripts/TileMapViewer.gd).
6. **Баг: граница на стыке двух провинций двоилась** — рисовалась НЕЗАВИСИМО
   по контуру каждой провинции; после раздутия (шаг 3) контуры соседей стали
   слегка отличаться → две разъехавшиеся линии на одном стыке. Пробовали
   рисовать границу по ИСХОДНОМУ (нераздутому) контуру — помогло не везде
   (у меньшинства провинций сам исходный контур соседей не совпадает
   координата-в-координату — реальная неточность реальных
   административных векторных данных, не баг).
7. **Механика границы переписана целиком** (предложение пользователя) — НЕ
   векторные линии по контуру вообще, а поиск границы ПРЯМО в уже залитой
   (раздутой, без щелей) картинке: где сосед другого цвета/прозрачности —
   там граница. Двоение линии физически невозможно по построению (один и
   тот же пиксельный переход). Толщина — сначала жёсткий `binary_dilation`
   (быстро оказалось плохо — см. ниже), потом заменено на плавное затухание
   по расстоянию (`scipy.ndimage.distance_transform_edt` + формула ТОЧНО как
   в `IrregularCellProvider._draw_segment`: `alpha=clamp((half_w+half_feather
   -dist)/feather, 0, 1)`), чтобы совпадать с живым слоем `8` визуально, а не
   просто жёстко перезаписывать пиксели.
8. **Ловушка с памятью (дважды)** — на дальнем зуме (`MIN_SCALE` задирает
   supersample) холст огромный (до ~16-17 тыс. px), но реальный контент —
   маленький уголок. `numpy`-обработка (int32/float маски) на ВСЁМ холсте
   падала по памяти (`ArrayMemoryError`, и отдельно — незаметное повышение
   до `float64` при смешивании с python-`float`, тоже удвоило память).
   Исправлено: работа numpy'ем только в обрезке по `img.getbbox()` (реальный
   контент), explicit `float32` везде.
9. **Известное НЕ РЕШЁННОЕ ограничение** (см. TODO.md) — реальные детали
   берега у́же самой линии границы (найдено: крошечный залив у Ла-Коруньи —
   та же точка нестыковки, что чинили в `world_ocean.json` выше; и заметнее —
   гавань у Лиссабона) отрисовываются сплошной чёрной кляксой, потому что
   линия границы физически шире самой детали. Обсуждали закрашивать мелкие
   детали цветом соседа — остановились, когда пользователь указал, что
   Лиссабонская гавань РЕАЛЬНАЯ и исторически значимая, закрашивать её
   нельзя не проверив размер. Более аккуратное решение (предложено
   пользователем) — детектировать такие "запечатанные" точки и локально
   раздвигать сушу до нужной ширины пролива (морфологическая операция по
   distance transform, наподобие conditional dilation/reconstruction) —
   сложнее всего, что делали в этой сессии, не начато.

Итог на тот момент: `assets/tiles_bundle/provinces_galicia_baked` — 22 тайла,
~0.9 МБ, тестовый регион Галисии, клавиша `4`, отдельно от живого слоя `8`.

## Узкие гавани решены, слой `4` расширен на всю Иберию, диск почищен (2026-07-10/11, продолжение)

### Механика границы в bake-скрипте дозрела ещё на шаг дальше пункта 8 выше
Между предыдущей записью и этой сессией плавное затухание по
`distance_transform_edt` (пункт 7 выше) заменено на бинарную дилатацию диском
(`scipy.ndimage.binary_dilation` по маске-диску радиуса `half_w+half_feather`)
— дешевле по памяти на полном тайле (EDT на float64/int64 требовал сотни МБ),
а само сглаживание края всё равно даёт downscale LANCZOS при сжатии
supersample-холста, так что визуальной разницы не видно. Плюс сам холст на
дальнем зуме больше не аллоцируется на весь тайл (было до ~17К px, падало по
памяти `MemoryError`/`ArrayMemoryError`) — теперь под каждый тайл берётся
только прямоугольник вокруг реального контента (bbox по всем `hits`,
выровненный на сетку supersample), а не весь холст целиком.

### Узкие гавани (Ла-Корунья и т.п.) — решены двумя мерами
1. **Раздутие `GAP_FIX_PX` (закрывает щели между соседними провинциями)
   перестало вылезать в море**: после `poly.buffer(GAP_FIX_PX)` из результата
   вычитается точный полигон мирового океана (`assets/world_ocean.json`,
   покрывает весь мир, не только тестовый регион — проверено по
   `build_world_ocean.py`, там нет региональных ограничений). Раньше раздутие
   без разбора грызло и сушу, и узкие заливы — теперь щели между соседями по
   суше закрываются как раньше, а к морю провинция возвращается к точному
   контуру.
2. **Граница на berегу теперь рисуется только внутрь суши**: маска
   `opaque` (непрозрачные исходные пиксели) накладывается на результат
   дилатации границы (`border &= opaque`) — чёрный пиксель границы физически
   не может лечь на воду. Раньше "полутолщины" границы с двух берегов узкого
   пролива смыкались и заливали его целиком сплошной чёрной кляксой.

Проверено пользователем визуально на клавише `4` в игре — залив у
Ла-Коруньи, который раньше был чёрной кляксой, теперь открыт. При разборе
скриншота с ложной тревогой про Лиссабон (он на самом деле открыт и не
пострадал — вне текущего региона на момент проверки) найден **один** остаточный
маленький чёрный треугольник южнее по побережью (примерно на широте
41.2–41.7°N, координаты не уточнены точно) — пользователь явно попросил
**оставить как есть**, не тратить время на него сейчас.

### Слой `4` расширен с Галисии на всю Иберию + Балеары
По прямой просьбе пользователя ("дорисуй слой 4 на всю Иберию"), после того
как оба фикса выше подтверждены на маленьком тестовом регионе:
- Скрипт переименован по смыслу (правило именования файлов из CLAUDE.md — имя
  "Галисия" стало враньём для региона, который теперь покрывает весь
  полуостров): `scripts/tools/bake_provinces_galicia_tiles.py` →
  `scripts/tools/bake_provinces_iberia_tiles.py`.
- `REGION_LONLAT` расширен с `(-10.5, 41.0, -6.0, 44.5)` (только Галисия/СЗ
  Иберии) до `(-9.9, 35.95, 4.4, 44.0)` — материковые Испания+Португалия+
  Андорра+Гибралтар и Балеарские острова (простым прямоугольником их не
  отсечь от материка — лежат в той же широтной полосе, поэтому просто
  включены), Канарские острова вне диапазона широт исключены сами по себе,
  южная граница 35.95°N подобрана чуть выше Сеуты/Мелильи (испанские анклавы
  в Африке, ~35.3–35.9°N), чтобы их не подхватило.
- Выходная папка тайлов переименована по тому же принципу:
  `assets/tiles_bundle/provinces_galicia_baked` →
  `assets/tiles_bundle/provinces_iberia_baked` (старая папка с 22 тайлами
  Галисии удалена — заменена).
- [TileMapViewer.gd](scripts/TileMapViewer.gd) обновлён: переменная
  `_provinces_galicia_layer_idx` → `_provinces_iberia_layer_idx`, путь к
  папке тайлов, название слоя в списке — `"Провинции (Иберия, запечённый)"`.
  Клавиша осталась `4`.
- Перезапечено: **59 тайлов, 5.04 МБ**, все 8 уровней зума (z0–z7),
  выполнилось без ошибок (~7.3 минуты, основное время — z6/z7 на большом
  регионе).

### Диск почищен: удалён осиротевший `elevcache` (969 МБ)
По прямой просьбе пользователя при подготовке к git — `assets/tiles_bundle/
elevcache` (данные высот Terrarium) не имел ни одного читателя с тех пор, как
старый слой "Клетки" (`CellTileProvider.gd`) был удалён 2026-07-09 (см. запись
выше "Ревизия проекта и чистка старых слоёв"), был только зафиксирован как
"осиротевший" в TODO.md, но не удалён. Удалён полностью, не просто исключён
из будущего git — данные всё равно никем не читались.

### Git-репозиторий — начато, ПОСТАВЛЕНО НА ПАУЗУ по просьбе пользователя
Обсудили с пользователем перенос проекта в приватный репозиторий на GitHub
(`gh` уже авторизован, аккаунт `k0ffeun1`). Посчитан реальный размер проекта
(~2.23 ГБ) и что в нём мусор, не нужный в git: `.godot/imported` (649 МБ,
кэш редактора, регенерируется), `scripts/tools/_work` (217 МБ, сырые
скачанные GIS-датасеты Natural Earth/GMRT, регенerируются скриптами),
`assets/tiles_bundle/tilecache` (113 МБ, дисковый кэш тайлов). После
исключения этого и удаления `elevcache` реальный контент — ~100–150 МБ,
обычный git без LFS. Пользователь подтвердил план (удалить `elevcache`
насовсем — сделано выше; пушить сразу в приватный репо на GitHub, не только
локально) **но остановил перед самим `git init`/push** — сказал, что
параллельно доделывает что-то в другой сессии. **Ничего из этого ещё не
сделано**: `.gitignore` не создан, `git init` не выполнен, коммитов нет,
удалённого репозитория нет. Продолжить, когда пользователь подтвердит, что
можно — см. TODO.md.

## Механика "3 уровня моря" (мелководье/шельф/глубины) — клавиша `5`, плюс мелководье насовсем в слое `2` (2026-07-10/11, продолжение)

### Батиметрия (клавиша `5`): найден и исправлен реальный баг с пропусками данных
Пользователь заметил белые крапинки на суше и чёрные точки на воде на
превью батиметрии. Причина — исходный GMRT-грид (склейка GEBCO+доп. съёмок)
реально содержит пропуски (NaN, ~0.09% сетки, дыры на стыках отдельных
съёмок), а `reproject()` в
[_preview_sea_depth.py](scripts/tools/_preview_sea_depth.py) не получал
`src_nodata`/`dst_nodata` — пропуски либо портили соседние пиксели мусорными
значениями от билинейного смешивания с NaN (чёрные точки), либо (раз `dst`
изначально нулевой) оставались как `elevation=0` (белые крапинки, ложно
классифицируется как мелководье). Исправлено: `src_nodata=np.nan`/
`dst_nodata=np.nan` в `reproject()`, оставшиеся NaN-пропуски заполняются
ближайшим валидным соседом (`scipy.ndimage.distance_transform_edt`).

### Живой инструмент "3 уровня моря" — родился, менялся несколько раз, финальная форма
Несколько итераций по прямым правкам пользователя за один день:
1. Сначала — 4 зоны глубины (мелководье/склон/глубина/бездна, пороги
   30/200/2000 м) статичной картинкой-превью.
2. Пользователь попросил живые слайдеры (расстояние/цвет) вместо
   перезапуска скрипта на каждое изменение — сделан шейдерный подход:
   глубина кодируется как 16 бит на пиксель (R/G-байты) в текстуре,
   `scripts/shaders/sea_depth_zones.gdshader` красит зоны в реальном
   времени по uniform-порогам/цветам.
3. Пользователь решил: "мелководье" должно быть НЕ по глубине, а по
   ФИКСИРОВАННОМУ расстоянию до берега (-1 км суша / +N км море, N менялся
   от 10 до 100 в процессе), не крутится слайдером. Остальные зоны
   (переименованы в "шельф"/"глубины моря", пользователь поправил
   орфографию "шлейф"→"шельф") остаются по реальной глубине,
   ОБЩИЙ цвет с мелководьем, редактируются слайдером.
4. Оба debug-слоя (батиметрия + мелководье) объединены в один класс
   [SeaZonesLayer.gd](scripts/SeaZonesLayer.gd) под одну клавишу `5` (были
   отдельными `SeaDepthPreviewLayer.gd`/`ShallowWaterBandLayer.gd` — оба
   удалены). Панель: общий цветовыбор "Мелководье/Шельф", один слайдер
   порога "Шельф/Глубины моря" (метры), отдельный цвет "Глубины моря".

**Финальная механика** (`LAND_MARGIN_KM=1.0`, `SEA_MARGIN_KM=20.0` в
SeaZonesLayer.gd, `DEFAULT_THRESHOLD_SHELF_M=300.0`):
- Мелководье — фиксированная полоса -1 км (суша) / +20 км (море) от берега,
  регион Иберия+Балеары (тот же, что у слоя `4`), НЕ по глубине.
- Шельф — 0..порог (по слайдеру) реальной глубины GMRT, только тестовый
  бокс Галисии (нет данных по глубине для всей Иберии), цвет = цвет
  мелководья.
- Глубины моря — глубже порога, свой цвет.

### Найден и исправлен реальный баг: щели между провинциями считались морем
При генерации растрового поля "расстояние до берега"
([build_coast_distance_field.py](scripts/tools/build_coast_distance_field.py))
суша сначала бралась напрямую из `assets/provinces.json` (union всех
полигонов региона) — но соседние провинции там местами не сшиты идеально
(микро-щели на стыках, тот же класс проблемы, что чинили в
`build_world_ocean.py` в прошлой сессии). Растеризатор помечал такую щель
"морем", и вокруг неё рисовался ложный ореол мелководья ПРЯМО ПОСРЕДИ
сплошной суши — пользователь заметил как "белые запятые" разбросанные по
дальним провинциям (изначально спутали с мелкими островами — острова
действительно тоже намеренно вырезаны фильтром `MIN_HOLE_AREA_PX2` в
`build_world_ocean.py`, см. память проекта, но это НЕ было причиной щелей на
суше). Исправлено — суша теперь считается как `bbox_региона.difference(
world_ocean_union)` (уже исправленный, гарантированно без щелей источник),
а не напрямую из `provinces.json`. Заодно это убрало и "конфетти" мелководья
вокруг мелких намеренно вырезанных островов.

### Найден и исправлен баг фильтрации текстуры (зубцы вдоль всего берега)
Расстояние до берега кодируется как 16 бит на пиксель, разбитые на 2
отдельных 8-битных канала текстуры (R/G — GPU не умеет упаковывать больше
8 бит в канал без специальных форматов). `LINEAR`-фильтрация спрайта
интерполирует R и G НЕЗАВИСИМО друг от друга, а не как единое число — там,
где младший байт (G) перескакивает через 255→0 (происходит каждые ~4 км
расстояния), интерполяция даёт мусорные комбинации, видимые как мелкие
зубцы/вздутия вдоль всего берега. Исправлено — `TEXTURE_FILTER_NEAREST`
вместо `LINEAR` на спрайте мелководья (та же настройка уже была у спрайта
батиметрии с самого начала).

### Мелководье перенесено НАСОВСЕМ в запечённый слой `2` (векторно, не растром)
Пользователь заметил рассинхрон береговой линии между слоем `2` (точный
вектор) и растровым мелководьем слоя `5` (8x supersample от того же
`world_ocean.json`, но с потерей мелких деталей берега при растеризации на
весь регион Иберия+Балеары) — предложил перенести мелководье прямо в
[bake_world_ocean_tiles.py](scripts/tools/bake_world_ocean_tiles.py), той же
векторной геометрией, что и сама заливка океана. Сделано: буфер суши в
честных км (не мировых px — исказило бы форму по широте, и не в градусах —
разная длина градуса на экваторе/у полюса) через `pyproj`, азимутальная
равнопромежуточная проекция (`+proj=aeqd`) с центром в центроиде региона —
`buffer(+SEA_MARGIN_KM).difference(buffer(-LAND_MARGIN_KM))`, обратно в
мировые px, рисуется в тех же PNG-тайлах, что и заливка/граница океана.
Граница слоя `2` и мелководья теперь совпадает пиксель в пиксель по
построению (одна и та же геометрия, один и тот же рендер-проход) — старый
растровый подход для мелководья
([build_shallow_water_band.py](scripts/tools/build_shallow_water_band.py),
удалён, заменён на `build_coast_distance_field.py` для живого слайдера на
`5`) устарел для финальной картинки, но остаётся полезен как живой
инструмент подбора значений.

Заодно слой `2` перепечён на `TILE_PX=1024`/`SUPERSAMPLE=8` (было 256/4) —
качество берега подтянуто до уровня слоя `4`, по прямой просьбе
пользователя ("берег заметно смазан рядом с векторно-точным мелководьем").

### Попытка перенести шельф/глубины В слой `2` ТОЖЕ — ОТКАЧЕНО обратно
Пользователь попросил слайдер прямо на слое `2` — объяснено, что у
запечённых статичных PNG-тайлов слайдер физически невозможен (нужен живой
рендер). Реализовано было и то, и другое: (1) сэмплирование реальной
батиметрии прямо в тайлы `bake_world_ocean_tiles.py` (функции
`_load_depth_raster`/`_draw_depth_zones` — код ОСТАВЛЕН в файле, но
отключён, `depth = None`), и (2) автоматическая привязка клавиши `2` к
показу живой панели слоя `5`. Оба варианта путали UX ("где слой 2???" —
пользователь ожидал заливку океана, а видел неожиданный слой из-за
побочного бага, см. ниже) — пользователь попросил откатить до состояния "слой
`2` = океан+мелководье насовсем, слой `5` = отдельная живая панель
шельф/глубины со слайдером", как было до этой попытки.

### Побочный найденный и исправленный баг: клавиши `2`/`9` открывали не те слои
При откате пользователь заметил — клавиша `2` открывала слой "Клетки моря
(тест)" вместо "Мировой океан". Причина не связана с мелководьем: в этой же
сессии раньше был добавлен новый слой "Клетки (Ла-Корунья, сетка)" (клавиша
`G`, см. запись про `build_cells_lacoruna_grid.py` выше) — он вставлен в
`_layers` ПЕРЕД "Клетки моря (тест)" и "Мировой океан", сдвинув их реальные
индексы в массиве на +1. `TileMapViewer.gd` привязывал `KEY_2`/`KEY_9` к
ЖЁСТКО ЗАПИСАННЫМ числам (`idx = 8`/`idx = 7`), а не к переменным — сдвиг
индексов их не обновил. Исправлено: `KEY_2` теперь ссылается на
`_ocean_layer_idx` (уже существовавшая переменная), для `KEY_9` заведена
новая `_sea_cells_layer_idx`, оба считаются динамически в момент добавления
слоя — тот же паттерн, что уже был у `KEY_4`/`KEY_G`. Урок: любой НОВЫЙ
слой, добавленный не последним в списке, ломает жёстко прописанные числа
клавиш дальше по списку — впредь заводить переменную сразу, не хардкодить
индекс.

### Итог сессии — что where сейчас
- Клавиша `2` (Мировой океан) — запечённые тайлы, ЗАЛИВКА + МЕЛКОВОДЬЕ
  (фиксировано, -1км/+20км) + граница, `TILE_PX=1024`/`SUPERSAMPLE=8`,
  регион СЗ Иберия/Галисия (тестовый, не весь мир).
- Клавиша `5` ("3 уровня моря") — живой debug-инструмент: мелководье
  (растровое, дублирует запечённое в `2`, но с живым цветовыбором),
  шельф/глубины моря (слайдер порога + 2 цвета, реальная батиметрия GMRT,
  только тестовый бокс Галисии).
- Порог шельф/глубины (300 м) — ПОДОБРАН НЕ ОКОНЧАТЕЛЬНО, дефолт, ещё не
  зафиксирован пользователем как финальное значение.
- Полный мир (все три уровня, весь `world_ocean.json`) — сознательно НЕ
  считать, пока тестовый регион не устроит полностью (тот же принцип, что и
  в прошлых записях).

## Разделение внешней/внутренней границы клеток на слое "C" (2026-07-11)

Пользователь спросил: можно ли на слое "C" (клавиша `C`, клетки-тест
Ла-Коруньи) сделать ВНЕШНЮЮ границу провинции видимой, а ВНУТРЕННИЕ границы
(между соседними клетками) оставить без контуров — и добавить UI-инструмент
регулировки толщины по аналогии с живой панелью слоя `2` (мелководье).

### Данные: новое поле `brd_boundary`
[build_cells_test.py](scripts/tools/build_cells_test.py) уже считал
`brd_open` — открытые цепочки рёбер клетки, которые НЕ лежат на границе
провинции (внутренние стыки). Старая функция `_split_open_border_chains`
переписана в `_split_border_chains`, которая теперь возвращает СРАЗУ ОБА
набора: `brd_open` (внутренние рёбра, как раньше) и новый `brd_boundary`
(рёбра, СОВПАДАЮЩИЕ с внешним контуром провинции — раньше просто
отбрасывались). `assets/cells_test.json` пересчитан скриптом заново.

### Рендер: `IrregularCellProvider.gd` — второй независимый проход границы
Провайдер и раньше рисовал "внутренний" border_src (`_border_color`/
`_border_width`, из `border_open_rings`/"brd_open"). Добавлен ВТОРОЙ,
полностью независимый проход в `_render()`: если конструктору передан
непустой `boundary_style: Dictionary`, для каждой клетки дополнительно
рисуются `border_boundary_rings` (из нового поля "brd_boundary") СВОИМ
цветом/толщиной/резкостью (`_boundary_color`/`_boundary_width`/
`_boundary_feather`/`_boundary_min_half_w`). Чтобы `_draw_segment` мог
рисовать не только "штатным" `_border_color`, у него появился необязательный
параметр `draw_color` (sentinel `Color(-1,-1,-1,-1)` = взять `_border_color`
как раньше). Два прохода геометрически НЕ пересекаются (данные изначально
разделены на этапе генерации), поэтому "мыла" от задвоения линий нет.

### `TileMapViewer.gd`: слой "C" переведён на постоянный живой рендер
Раньше слой "C" при наличии `assets/tiles_bundle/cells_test_baked`
использовал запечённые PNG (`BakedTileProvider`) для картинки и отдельный
живой `IrregularCellProvider` только для клика по клетке. Бейк-скрипт
(`bake_cells_test_tiles.py`) вообще не рисует границ (только заливку) — на
время подбора значений ползунком это не годилось, поэтому слой "C" переведён
на ВСЕГДА живой рендер (тот же провайдер и для картинки, и для клика) — тот
же паттерн, что уже был у чернового слоя "G" (без бейка вовсе). Данных мало
(4 клетки), живой рендер не создаёт заметной нагрузки.

В `BORDER_STYLE` (в начале файла) добавлен независимый стиль
`"cell_boundary"` (внешняя граница провинции для слоя "C") — отдельно от
`"cell"` (граница МЕЖДУ клетками, тот же стиль ещё используется и черновым
слоем "G").

### Временная живая панель — построена, потом снесена по просьбе пользователя
Сначала была собрана панель "Граница провинции (слой C)" (по образцу
`_build_ocean_shallow_panel` слоя `2`: `ColorPickerButton` + `HSlider`
поверх живого провайдера, правка вызывала `set_boundary_style`/
`set_inner_style` — сброс кэша тайлов провайдера + принудительная выгрузка
уже показанных спрайтов этого слоя, чтобы новый стиль подхватился без
перемещения камеры).

**Найденный и исправленный по пути баг**: ползунки не реагировали на мышь,
но ТОЛЬКО когда слои `2` (Мелководье) и `C` были включены ОДНОВРЕМЕННО.
Причина — панели `_ocean_shallow_panel` (y: 220–680) и новая панель слоя "C"
(первая версия, y: 540–780) пересекались по прямоугольнику в $UI; более
ПОЗДНО добавленный в дерево Control (панель слоя `2`, т.к. её код в
`_ready()` идёт позже) перехватывал клики поверх более раннего, даже когда
визуально казалось, что видна только нижняя панель. Панель слоя "C" была
сдвинута ниже (y: 690–1075), чтобы прямоугольники панелей никогда не
пересекались ни при каком сочетании включённых слоёв.

Дальше пользователь подобрал вживую нужные значения и попросил панель
убрать, зафиксировав числа как константы — сделано:
- `BORDER_STYLE["cell_boundary"]` (внешняя граница провинции): чёрный,
  `width=0.250`, `feather=3.0`.
- `BORDER_STYLE["cell"]` (границы МЕЖДУ клетками): чёрный, `width=0.100`,
  `feather=3.0` (было `alpha=0`, т.е. невидимо, — теперь видимо и настроено).

Панель (`_build_cells_boundary_panel`), её переменная (`_cells_boundary_panel`),
синхронизация видимости в `_process` и вспомогательная функция
`_purge_cells_test_tiles` — удалены за ненадобностью (константы применяются
один раз при загрузке, живая правка больше не нужна). Ставшие мёртвым кодом
`IrregularCellProvider.set_boundary_style`/`set_inner_style` тоже удалены.
Слой "C" остался на постоянном живом рендере (см. выше) — бейк для него
сейчас не актуализирован под новую границу.

## Глубина моря: непрерывный градиент + изобаты вместо 2 жёстких зон (2026-07-11)

Живой оверлей глубины (`sea_depth_zones.gdshader`, клавиши `2`/`5`) раньше
красил море ДВУМЯ зонами через жёсткий порог (`threshold_shelf_m`). Заменено
на:

- **Непрерывный градиент по 3 цветам** — `color_shelf` (0 м) →
  `color_mid` (склон, положение `mid_point` на кривой, по умолчанию 0.70) →
  `color_deep` (max_depth_m), с кривизной `gradient_gamma` (по умолчанию
  0.80, растягивает мелководье по палитре, как на морских картах).
- **Изобаты** — тонкие линии через `isobath_interval_m` (по умолчанию 50м),
  антиалиасинг через `fwidth`. Код НЕ удалён, но по прямой просьбе
  пользователя 2026-07-11 инструмент скрыт из обеих панелей
  (`isobaths_row.visible = false`) и выключен по умолчанию
  (`show_isobaths = false`) — "не удаляй полностью, задокументируй, чтобы
  просто не было видно, мб пригодится в будущем".
- Старые слайдеры "порог"/"ширина перехода" (`threshold_shelf_m`,
  `transition_m`) убраны из UI обеих панелей (слой `2` в `TileMapViewer.gd`,
  слой `5` в `SeaZonesLayer.gd`) — заменены на кривизну градиента + цвет/
  положение склона.
- Полоса "мелководье" (`shallow_water_band.gdshader`) — плавный градиент
  ТОЛЬКО со стороны моря (`edge_transition_km`, диапазон слайдера расширен
  до 100 км), край со стороны суши остался резким обрезом (`step`) по прямой
  просьбе пользователя (сначала перепутал стороны местами — переспросили и
  исправили).
- Зафиксированные пользователем значения по умолчанию (панель слоя `2`):
  цвета мелководье `#36b2dc`, шельф `#009acd`, склон `#04588c`, глубина
  `#062962`; заход на сушу 2.0км, ширина в море 15.0км, плавность края
  12.1км, кривизна градиента 0.80, положение склона 0.70, шаг изобат 50м
  (сохранён в коде, хоть инструмент и скрыт).

### Регион глубины (`scripts/tools/_preview_sea_depth.py`) — расширяли и откатили

Тестовый бокс глубины держали НЕ равным `TEST_REGION_LONLAT` генератора
клеток и не полному Пиренейскому п-ову — отдельная история:

1. Было: Северо-Запад Иберии/Галисия `(-10.5, 41.0, -6.0, 44.5)`.
2. Расширили западную границу в Атлантику до `-16.0` (тот же скрипт,
   `gmrt_galicia_atlantic.tif`, 158МБ) — чтобы сравнить градиент/изобаты на
   бОльших глубинах открытого океана, а не только на узком шельфе у берега.
   Макс. глубина в регионе выросла с ~300м (узкий шельф) до ~5819м.
3. Пробовали расширить до "весь Пиренейский п-ов + Балеары
   `(-9.9, 35.95, 4.4, 44.0)` + буфер 450км во все 4 стороны" —
   `(-15.2, 31.9, 9.7, 48.0)`, `gmrt_iberia_450km_buffer.tif` (417МБ,
   текстура 4533×3854px). **ОТКАЧЕНО обратно тем же днём** — результат
   заливал весь экран сплошной синевой без пользы для подбора порогов, плюс
   найден и исправлен попутный баг (при переключении региона забыли
   поменять `SRC_TIF` на новый файл — скрипт брал старый маленький растр
   Галисии, а всё за его пределами замазывал `nearest-neighbor`-заливкой
   крайних значений, отсюда видимый шов на скриншоте).
   `gmrt_iberia_450km_buffer.tif` НЕ удалён по прямой просьбе пользователя —
   пригодится, когда вернётся очередь до этого региона (вернуть
   `REGION_LONLAT = (-15.2, 31.9, 9.7, 48.0)` и поменять `SRC_TIF`).
4. Вернулись к состоянию п.2 (Галисия+Атлантика, `-16.0..-6.0, 41.0..44.5`).

**Оценка масштаба на будущее (весь мир)**: 417МБ для бокса
24.9°×16.1°≈401 кв.град линейно даёт ~64ГБ для всего мира (360°×~170°) —
непрактично одним GMRT-запросом, к тому же сервис вряд ли отдаст такой объём
за раз. Для полного мира глубину придётся тайлить (как слой "Мировой
океан"), а не одной растровой текстурой — отдельная задача на будущее.

### Слой "9" ("Клетки моря (тест)") — удалён полностью

По прямой просьбе пользователя: код слоя в `TileMapViewer.gd`
(`_sea_cells_layer_idx`, блок создания `IrregularCellProvider` из
`sea_cells.json`, привязка `KEY_9`) и файлы
`assets/generated/sea_cells.json`/`sea_cell_graph.json` — удалены. Генератор
`scripts/tools/generate_sea_cells.py` (см. `АРХИТЕКТУРА_МОРСКИХ_КЛЕТОК.md`)
НЕ удалён — при возврате к теме морских клеток нужен АБСОЛЮТНО НОВЫЙ слой
(явное решение пользователя), не переиспользование старого тестового.
Проект без git — удаление файлов необратимо, поэтому перед удалением
уточнялся явный список того, что стирается.

## Главные города провинций (слой "4", Иберия) (2026-07-12)

Точка-маркер + подпись на главный (по населению) реальный город каждой
провинции региона Иберия, включается вместе со слоем "Провинции (Иберия)"
клавишей `4`.

- **Источник городов**: Natural Earth `ne_10m_populated_places` (скачан и
  сконвертирован в geojson через pyshp, `scripts/tools/_work/ne_10m_populated_places.geojson`,
  сам файл НЕ в git, см. `_work/`).
- **Подбор города на провинцию**: [build_province_cities_iberia.py](scripts/tools/build_province_cities_iberia.py)
  — point-in-polygon по контуру провинции (`assets/provinces_iberia.json`),
  из попавших внутрь точек берётся с максимальным `POP_MAX`. Если ни одна
  точка не попала внутрь — ближайшая точка в пределах `NEAREST_FALLBACK_PX`,
  но ТОЛЬКО если она не принадлежит уже какой-то ДРУГОЙ провинции (иначе
  провинция без своего города в датасете "крала" бы маркер соседней —
  найденный баг: Луго осталась без видимого маркера, потому что её fallback
  без этой защиты уводил на Оренсе, который уже законно принадлежал
  провинции Ourense).
- **`MISSING_CITY_OVERRIDES`** — 21 провинциальный центр, которого физически
  НЕТ в `ne_10m_populated_places` (10m-разрешение слишком грубое для таких
  городов: Луго, Сеговия, Сория, Уэска, Теруэль, Самора, Авила, Сьюдад-Реаль,
  Куэнка, Касерес, Жирона, Льейда и несколько французских/алжирских
  департаментов). Координаты (lon/lat) введены вручную по общим знаниям
  географии, БЕЗ сверки по внешней базе данных — точность не выверялась
  (по прямой просьбе пользователя: "не важна точность"). Если когда-нибудь
  понадобится точность — сверить по GeoNames (`cities15000.txt`), это
  обсуждали как альтернативный источник городов.
- **`CITY_POSITION_OVERRIDES`** — вручную сдвинутые позиции (не "нет в
  датасете", а "датасет указывает не туда"):
  - "La CoruÃ±a" → маяк/полуостров Torre de Hércules, а не современный
    центр южнее (реальный исторический порт стоял на самом полуострове).
  - "Sevilla" → к новому побережью (см. ниже про `patch_sevilla_coastal_access.py`),
    а не к реальному инленд-центру города.
- **Прижимание к берегу** (`_snap_cities_to_coast`): город, чей центр НЕ
  касается моря, но лежит в диапазоне `(TOUCH_EPSILON_KM=2 .. SNAP_TO_COAST_KM=12]`
  км от береговой линии (`assets/world_ocean.json`) — переносится на сам
  берег (ближайшая точка береговой линии + маленький отступ `INLAND_NUDGE_PX`
  вглубь суши, чтобы не встать ровно на границу вода/земля). Порог 12км —
  прямое решение пользователя (проверено на примере Бильбао, 10.1км от
  берега). Настоящие внутренние города дальше порога (Тизи-Узу, Сантарен и
  т.п., ~17-20км) не трогаются — иначе уехали бы на побережье без всякого
  исторического основания.
- **Ручная правка географии: Севилья получила выход к морю**
  ([patch_sevilla_coastal_access.py](scripts/tools/patch_sevilla_coastal_access.py)) —
  у провинции "Sevilla" в реальных административных границах НЕТ берега
  (~80км вверх по Гвадалквивиру от Атлантики), но исторически (с XV века,
  до заиливания реки) Севилья была ГЛАВНЫМ атлантическим портом Испании. По
  прямой просьбе пользователя — простой прибрежный клин земли (bbox по lon/
  lat в районе устья Гвадалквивира) вырезан из "CÃ¡diz"/"Huelva" и присоединён
  к "Sevilla" (не повторяли форму русла реки — первая версия с этим была
  откачена по просьбе пользователя, "давай проще"). Правка внесена прямо в
  `assets/provinces.json` (источник истины), `provinces_iberia.json`
  пересобран через `build_provinces_iberia.py`.
- **Слой "Провинции (Иберия)" переключён на живой рендер**
  (`PROVINCES_IBERIA_FORCE_LIVE = true` в `TileMapViewer.gd`) — запечённые
  тайлы (`bake_provinces_iberia_tiles.py`) по умолчанию кроют только тестовый
  регион СЗ Испании, не всю Иберию, поэтому не показали бы южную правку
  (Севилья/Кадис/Уэльва). Перепекать под полный регион дороже, чем просто
  рисовать живьём.

### Отрисовка маркера — векторные узлы, НЕ тайлы (перепройдено дважды)

Первая версия ([ProvinceCityMarkerProvider.gd](scripts/ProvinceCityMarkerProvider.gd),
УДАЛЁН) рисовала маркер растром прямо в тайлы 256×256, как границы
провинций. Оказалось плохой идеей для ~100 РЕДКИХ точек: на стыке двух
тайлов круг иногда проваливался (половина рисовалась в одном тайле, половина
в другом — если решение "попадает ли маркер в тайл" на границе давало разный
результат для соседних тайлов), из-за чего часть городов выглядела
"пустыми", а сквозь непрорисованную половину круга просвечивала заливка
провинции под ним ("розовый цвет заходит на круг").

Исправлено переходом на векторные `Node2D` (тот же приём, что уже был у
подписей морей, `SeaLabelsLayer`/`SeaLabelNode` — подписей/маркеров мало, не
нужен тайловый механизм):
- [ProvinceCityMarkerNode.gd](scripts/ProvinceCityMarkerNode.gd) — один
  узел = один город, круг через `draw_circle(..., antialiased=true)` +
  подпись `draw_string` с отступом `LABEL_GAP=8.0` px ПОД кругом (не
  слитно), `z_index=100` (гарантированно выше всех тайловых слоёв).
- [ProvinceCityMarkersLayer.gd](scripts/ProvinceCityMarkersLayer.gd) —
  контейнер, грузит `province_cities_iberia.json` один раз, создаёт узлы,
  каждый кадр выставляет `scale = 1/zoom` (постоянный размер на экране при
  любом зуме, тот же приём, что `SeaLabelsLayer._process`). Видимость
  синхронизирована со слоем "Провинции (Иберия)" в `TileMapViewer._process`.
- `SeaLabelNode.gd`/`SeaLabelsLayer.gd` попутно получили необязательный
  `offset_y` (по умолчанию 0, подписи морей не тронуты) — использовался в
  промежуточной версии, когда подписи городов ещё были через переиспользование
  `SeaLabelsLayer`; после перехода на `ProvinceCityMarkerNode` подписи городов
  рисуются им же самим, но параметр `offset_y` оставлен в `SeaLabelNode` как
  общая возможность (не удалён — рабочий, может пригодиться).
